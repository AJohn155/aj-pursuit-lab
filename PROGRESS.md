# Progress log

Append-only. Read this and SPEC.md before starting any session.

---

## 2026-07-02 — P0 Scaffold

**Model:** Claude (Sonnet 4.6), via Claude Code CLI

**Completed:**
- Vite + React 19 + TypeScript scaffold (`npm create vite@latest`, `react-ts` template)
- Tailwind CSS v4 via `@tailwindcss/vite` plugin
- `react-router-dom` for client-side routing, `BrowserRouter` with `basename="/aj-pursuit-lab"`
- Tab shell (`src/components/TabShell.tsx`): left rail on desktop, bottom bar on mobile, all 10 tabs from §5 (Rides, Compare, Adjuster, Watts to Win, Gains, Pacing, Race Day, Calculators, Records, Settings)
- Empty page stub per tab under `src/pages/<Tab>/index.tsx`
- Repo layout scaffolded per §2.1: `src/engine/`, `src/engine/__tests__/`, `src/store/`, `data/fixtures/` (empty, see its README)
- GitHub Pages deploy workflow (`.github/workflows/deploy.yml`): builds on push to `main`, deploys via `actions/deploy-pages`
- SPA fallback for GitHub Pages: `public/404.html` (encodes deep link) + decode script in `index.html` (rafgraph spa-github-pages technique), `pathSegmentsToKeep = 1` for the `/aj-pursuit-lab/` repo path
- Repo visibility changed private → public (owner confirmed; required for GitHub Pages without a paid plan)

**Test status:**
- `npm run build` passes clean (tsc -b && vite build)
- All 10 tab routes verified 200 against local dev server; all 10 tab labels confirmed present in built JS bundle
- Deployed and live: **https://ajohn155.github.io/aj-pursuit-lab/** — verified via curl: root HTML (200), JS/CSS bundle assets (200), all 10 tab labels present in the live bundle, and the `404.html` SPA-fallback redirect confirmed serving on a deep link (`/rides`)
- No visual browser verification was possible in this environment (no Chrome MCP connection, computer-use access declined) — verification was route/build/asset-level via curl, not visual. Recommend the owner open the live URL on phone and desktop to confirm the P0 gate visually.

**What's next:** P1 Data layer — Dexie schema, DataStore interface, Firebase init/auth/sync, settings page, venue seed + manager, backup export/import.

**Deviations from SPEC (explicit):**
- Tailwind CSS v4 used (CSS-first config via `@import "tailwindcss"` in `src/index.css`, no `tailwind.config.js`/`postcss.config.js`) rather than v3 — SPEC didn't pin a version; v4 is current stable and simplifies config.
- Router library not specified in SPEC; used `react-router-dom` v7 as the standard choice for tab/route navigation.
- Development environment had no Node.js, no Homebrew, and no GitHub CLI preinstalled; Node was installed as a portable tarball under `~/.local/node` (not a system-wide install) to build/test locally.

---

## 2026-07-03 — P1 Data layer

**Model:** Claude (Sonnet 4.6), via Claude Code CLI

**Completed:**
- Data model types per §3 (`src/store/types.ts`): `Settings` (singleton), `Venue`, `Ride`, `Scenario`, `Event`, all extending a common `Persisted` (`id`/`createdAt`/`updatedAt`) shape
- Dexie schema (`src/store/db.ts`): tables for settings/venues/rides/scenarios/events; `ensureSeeded()` seeds the default `Settings` singleton (§3.1 values) and all 8 venues (§3.2 table) on first run, idempotently
- `DataStore` interface (`src/store/DataStore.ts`): generic `Collection<T>` (get/getAll/put/delete/observe) per collection, backed today by Dexie; a `put(item, { silent })` escape hatch lets the sync engine apply incoming remote docs without re-triggering an outbound push; `onChange` lets the sync layer observe local writes without coupling to Dexie directly — this is the swap point called out in §2
- Firebase init (`src/store/firebase.ts`) with the owner-supplied config, Google `signInWithPopup` auth, Firestore client
- Sync engine (`src/store/sync.ts`): on sign-in, does a one-time full reconcile per collection against `users/{uid}/{collection}` (via `planMerge`), then keeps a live `onSnapshot` listener per collection (applies remote-wins docs silently) and a local `onChange` listener (pushes local-wins docs) — last-write-wins on `updatedAt`, all reads local per §2
- Pure, unit-tested last-write-wins merge logic (`src/store/merge.ts`): `resolveDoc` (single-doc conflict resolution) and `planMerge` (full-collection reconcile plan)
- `firestore.rules` per §6, committed to the repo; owner manually published it via the Firebase console (no admin/service-account credentials were available to this session to deploy it via CLI/API)
- Settings page (`src/pages/Settings/`): `AuthPanel` (Google sign-in/out + status), `GlobalParams` (editable §3.1 fields with provenance captions, writes on blur), `BackupPanel` (full-DB JSON export/import), `VenueManager` (CRUD over all venue fields; editing `lapLengthM` or `bendRadiusM` auto-recomputes `straightLengthM` to close the §3.2 constraint, editing `straightLengthM` directly leaves a visible residual instead)

**Test status:**
- `npm test` (vitest): 16/16 passing — 6 tests on `resolveDoc`/`planMerge` pure logic (tie-breaking, local-only/remote-only/conflicting docs, mixed batches, true no-op), plus new integration-style tests against a real (via `fake-indexeddb`) IndexedDB: seeding is correct and idempotent, `DataStore` CRUD round-trips, `onChange` fires on put/delete but not on `{ silent: true }` puts
- `npm run build` passes clean; `npm run lint` passes clean (had to restructure `GlobalParams` to avoid a `setState`-in-effect anti-pattern the lint config now flags — settled on a key-based remount instead of a synced draft state)
- **Not verified by me:** the actual cross-device definition of done ("sign in with Google on the deployed site, edit a venue on my laptop, see the change on my phone after reload"). I have no browser or computer-use access in this environment (declined twice this session) — all verification above is build/type/lint/test-level. The owner needs to do this check personally.
- Firebase project state confirmed before building: Firestore database exists, Google sign-in provider enabled, `aj-pursuit-lab.firebaseapp.com`/`ajohn155.github.io` are authorized domains — all confirmed by the owner or via public config endpoints, not assumed.

**What's next:** P2 Engine core — §4.1–4.3, 4.9–4.14 as pure functions with unit tests (synthetic data), calculators' math functions with gate-7 tests.

**Deviations from SPEC (explicit):**
- §5.10 lists CP/W′ settings and the start-profile template as part of the Settings page; both depend on engine functionality (§4.10, §4.13) that doesn't exist yet, so they're deferred to whichever phase builds that engine surface (P2/P6/P7) rather than stubbed now.
- The venue "fit-geometry button" (§5.10, §4.8) is also deferred — venue geometry fitting requires ride lap data and the physics engine, neither of which exist yet in P1.
- Firestore tombstones (remote deletes) are not synced in this pass: local deletes remove the local doc but don't currently propagate a delete to Firestore. SPEC doesn't specify delete-sync semantics; flagging so it's a conscious gap, not an oversight, when ride/scenario deletion UI lands in later phases.
- Backup import is additive/merge-by-put, not a destructive restore — importing a backup won't remove local docs absent from the file. SPEC's "export/import full backup JSON" wording didn't specify restore semantics; merge felt safer as a default than silently deleting local data.

---

## 2026-07-03 — P1 fix: venue sync race + stale UI

**Model:** Claude (Sonnet 4.6), via Claude Code CLI

Owner reported: global params sync correctly phone↔laptop, but venue edits don't seem to. Two real bugs, found by tracing the sync path rather than reproducing directly (no browser access in this environment):

1. **Seed timestamp race (`src/store/db.ts`).** `ensureSeeded()` stamped seed settings/venues with `new Date().toISOString()` — "now" — at first load. A device's *first-ever* app load seeds locally before it has synced with anyone. If that first load happens after a genuine edit was already pushed from another device, the fresh (but unedited) seed data carries a *later* timestamp than the real edit, so last-write-wins picks the stale seed data and overwrites the edit in Firestore. This is why settings appeared to work (the owner's devices had likely already synced once before that test, so the race didn't fire) while venues, tested with a device syncing for the first time, hit it. Fix: seed docs now get a fixed, deliberately-ancient `updatedAt` (`1970-01-01T00:00:00.000Z`) instead of "now", so any real edit — which always carries a current timestamp — wins regardless of seeding order across devices. Added a regression test (`db.test.ts`) simulating exactly this two-device race.
2. **Stale uncontrolled inputs (`VenueManager.tsx`).** Venue edit fields are uncontrolled (`defaultValue`), and `VenueRow` was keyed only by `venue.id`, which never changes. An incoming sync updates the underlying data correctly, but React doesn't remount the row, so the visible input values could stay stale in an already-open tab. Fixed by keying on `` `${v.id}-${v.updatedAt}` `` so a row remounts (and its inputs re-initialize from fresh data) whenever the venue's `updatedAt` changes.

**Note for the owner:** if your earlier venue edit was already overwritten by this race before the fix deployed, you'll need to re-apply it once this is live — the fix prevents the bug going forward but doesn't recover data already lost to it.

**Test status:** `npm test` 17/17 passing (new regression test included); `npm run build` and `npm run lint` both clean.

---

## 2026-07-03 — P2 Engine core

**Model:** Claude (Opus 4.8), via Claude Code CLI

**Completed:** the pure-TypeScript physics engine under `src/engine/` (SPEC §4.1–4.3, §4.9–4.14) plus the four calculator math functions (§5.8). `ENGINE_VERSION = '0.2.0-p2'`. No React/DOM/store/dexie/firebase imports anywhere in `src/engine` (purity rule §2.1 verified by grep).

- `constants.ts` / `params.ts` — g, ENGINE_VERSION; mEff = massKg+rotatingMassEqKg, effectiveCrr (§4.1).
- `atmosphere.ts` — air density from T/P/RH, owner's convention verbatim (§4.2); fast-mode steady + lap-1 density scales (§4.12).
- `track.ts` — track position model (§4.3): bendFraction, isBend segment map, leanAngle, kN, kV, cornerFactors, lapCrrMultiplier, and the wheel→COM speed inversion.
- `cda.ts` — whole-lap energy-balance CdA (§4.9): energyBalanceCda (returns full E_in/ΔKE/E_roll/E_aero breakdown), cdaPerLap, cdaRace with 95 % CI from lap scatter, sane-range predicate.
- `simulate.ts` — forward simulator (§4.10): semi-implicit Euler at dt=0.1 s over the track model, lap-split interpolation, default standing-start power template.
- `solve.ts` — bisection inverse solvers for power/CdA/crr/mass/rho vs a target time; Watts-to-Win (power-to-match + ΔCdA alternative) (§4.11).
- `density.ts` — fast-mode lap-vector normalization + full-mode re-simulation + equivalent-time-at-ref-density (§4.12).
- `wprime.ts` — Skiba W′ balance with the spec τ, plus CP/W′ estimation from mean-maximal points (§4.13).
- `pacing.ts` — 3-parameter pacing-family grid search under W′bal ≥ 0, and actual-vs-optimal comparison with per-lap time lost (§4.14).
- `calculators.ts` — cadence (+grid), power-for-speed flat & full-track modes, watts-saved (+grid), time adjuster (env→ρ→scale) (§5.8).

**Test status:** `npm test` **99/99 passing** (11 files; 82 new engine tests + the 17 pre-existing store tests); `npm run build` and `npm run lint` both clean.
- **Gate 6 (air density) — full:** `airDensity(24,1006,55) = 1.1722` (within 1e-4 of target).
- **Gate 7 (calculators) — full:** power-for-speed@63 kph = 684.29 W; watts-saved@60 kph,5 counts = 13.31 W; time-adjuster 15.6 s @ρ1.1722→0.9934 = 14.7627 s (target 14.7626). All within spec tolerance.
- **Gates 4 & 5 — synthetic** (fixture `.fit` ingest is P3, so these are synthetic-data forms): CdA energy balance recovers the ground-truth CdA to ~1e-8 on constant-COM-speed data and to ~1e-5 on a constant-power interior lap from the forward sim; forward-sim/solver round-trips recover input power and CdA; two synthetic rides land in [0.16,0.26] and within 0.015 m² of each other.
- **Gates 1–3** (parse, race detection, lap construction) depend on §4.4–4.8 and are deferred to P3 with the real fixtures.

**What's next:** P3 Ingest — §4.4–4.8 (FIT parse → 1 Hz timeline, race detection, standing-start reconstruction, lap construction/calibration, venue geometry fitting) + detection-confirm UI, run against the two real fixtures for gates 1–5, and add `data/fixtures/expected.json`.

**Deviations / documented judgment calls (SPEC §9):**
- **Scope:** §4.4–4.8 intentionally NOT built here — they're the P3 phase and gates 1–3 depend on them. Gates 4/5 done in synthetic form; 6/7 in full. This matches P2's charter in §8.
- **Wheel→COM inversion (§4.3/§4.9):** the spec gives `v_com = v_wheel/kV(v,s)` but kV is defined via COM speed, so the inversion is a fixed-point iteration (6 iterations; converges to ~1e-10 because kV≈1.03). Documented in `track.ts`.
- **CI estimator (§4.9):** "95 % CI from lap-to-lap scatter" implemented as the normal approximation 1.96·SD/√n. Spec asks for a 95 % CI but not a specific estimator; normal approx keeps the engine dependency-free.
- **W′ recovery form (§4.13):** spec gives the recovery τ but not the update; used the canonical Skiba exponential relaxation toward W′, `W′bal ← W′ − (W′−W′bal)·e^(−dt/τ)`. Depletion is the exact spec form.
- **Pacing family (§4.14):** the three FREE parameters are (startMult, settleW, endKickFrac) per spec; the opening length (1.5 laps) and end-kick magnitude (×1.10) are fixed structural constants so the search space stays 3-D. Documented in `pacing.ts`.
- **Integrator (§4.10):** semi-implicit (symplectic) Euler at the spec's dt=0.1 s (chosen for stability near the standing start without changing the step); a MAX_ACCEL guard and a propulsion speed-floor tame the v→0 singularity at the start. Absolute ±1.5 s fixture reproduction (gate 5) is verified in P3 with real power series.
- **Geometry closing:** `makeTrack(L,R)` derives the straight length that closes `L = 2S + 2πR` (§3.2). The engine assumes a closing geometry; the store reconciles a venue's published S/R residual before calling the engine. (Note: the seed venues' placeholder R=23/S=42 do NOT close — that's expected per §3.2 and handled at the store/UI layer.)
- **Full-track-model power (§5.8):** interpreted as the lap-average power to hold a constant COM speed — aero unchanged by cornering, rolling lifted by the lap-averaged kCrrLap. Reduces to the flat equation on a straight track.

---

## 2026-07-03 — P3 Ingest

**Model:** Claude (Opus 4.8), via Claude Code CLI

**Completed:** the full FIT-ingest pipeline (SPEC §4.4–4.8) under `src/engine/ingest/`, the two real fixtures wired into automated gate tests, and the detection-confirm UI. `ENGINE_VERSION` bumped `0.2.0-p2` → `0.3.0-p3` (CdA formula changed — see the wheel-path fix below). All ingest is pure TS; the only new dependency, `fit-file-parser`, is DOM-free and isolated in `fit.ts`.

- `fit.ts` — `fit-file-parser` adapter → record-level power/speed/distance/cadence/temperature (§4.4). Verified both fixtures: quali 326 records (first ≈8.63 m/s, 18.8 m, P=0), final 332.
- `timeline.ts` — 1 Hz timeline over the race segment; gaps ≤5 s interpolated (flagged), >5 s split into segments (race = longest); dropout stats for the quality badge (§4.4).
- `detect.ts` — race detection (§4.5): 10 s>250 W window (150–350 s), start anchor (walk back to v<1, else missingStart), t0 refine (linear/jerk-free extrapolation to v→0, clamp ≤3.5 s), finish = 4000 m datum crossing.
- `laps.ts` — calibration + 16 lap boundaries (§4.7); reports interior-14-lap factor c and per-lap line height; builds calibrated-datum-speed sample groups for the CdA.
- `start.ts` — standing-start energy reconstruction (§4.6).
- `geometry.ts` — venue geometry fitting (§4.8): R+phase grid-search matching the kV shape to the band-passed speed.
- `analyze.ts` — end-to-end `analyzeRide()` → detection, calibration, CdA (laps 3→16), start metrics, sim reproduction.
- `src/components/SpeedTrace.tsx` + `src/pages/Rides/DetectionConfirm.tsx` — drop `.fit` → detect → speed trace with draggable start/finish handles + official-time field + one-click confirm (§4.5 / §5.1).
- `data/fixtures/expected.json` — machine-readable gates; `src/engine/__tests__/fixtures.test.ts` asserts gates 1–5 against the real files; `ingest.test.ts` covers timeline/start/geometry.

**Test status:** `npm test` **115/115 passing** (13 files); `tsc -b`, `npm run build`, `npm run lint` all clean. Fixture gates 1–5:

| Gate | Quali | Final | Threshold | ✓ |
|---|---|---|---|---|
| 1 parse / 1 Hz / dropout | 326 rec, 318 s, 0 s | 332 rec, 343 s, 23 s | built + stats | ✓ |
| 2 detection Δ vs official | −1.20 s (missingStart) | +1.07 s (v≈2.58) | ≤ 2.5 s | ✓ |
| 3 lap count / calibration c | 16 / 0.9959 | 16 / 1.0041 | 16 / [0.99,1.01] | ✓ |
| 4 CdA | 0.1694 | 0.1676 | [0.16,0.26], Δ<0.015 (=0.0018) | ✓ |
| 5 sim reproduction Δ | −0.85 s | −0.82 s | ≤ ±1.5 s | ✓ |

**What's next:** P4 Rides — upload flow (drop→confirm→metadata→save&analyze), CSV import, rides list, ride detail with all charts (introduce the Plotly chart wrapper here per §2).

**Deviations / documented judgment calls (SPEC §9):**
- **⚠ Wheel-path double-count between §4.7 and §4.9 (flagged to owner, approved).** As written, §4.7 calibration `c = 4000/dRaw` already absorbs the full wheel-vs-datum path excess (lean + line height), and §4.9 then divides speed by `kV` again — double-counting the lean excess by ~2%. With the literal spec the forward sim (§4.10, the physical ground truth) runs 5–8 s slow, so **gate 5 fails** (CdA also inflated to 0.179/0.183). Fix: calibration owns the wheel→datum conversion; the CdA uses the COM datum speed `v_com = c·v_wheel` and drops the `/kV`. Steady-window reproduction then goes from +4.6/+6.3 s to +0.9/+0.6 s, all gates pass, CdA = 0.169/0.168. Owner confirmed this matches his own iterative calcs (~0.164–0.17 with Crr 0.0014) and approved the deviation. Implemented in `cda.ts` (Sample now carries `vCom`); `kN` rolling lift retained (a separate effect). Consequence: **comHeightM no longer affects CdA** (it lived only in the dropped `kV` term).
- **Detection finish = 4000 m datum crossing, not power-collapse (§4.5.3).** Power-collapse runs ~3.6 s long on the final (rider pedals ~56 m past the line), which fails gate 2; the 4000 m crossing is the timed pursuit distance and what officialTimeS / the lap-16 boundary / §4.5.5 alignment reference. Power-collapse remains the coarse window detector.
- **Gate-5 reproduction handles the under-recorded start.** The SRM reads ~0 W while the rider is already accelerating off the gate, so simulating from a standstill with raw power starves acceleration. Reproduction instead uses the measured (real) elapsed time to the first valid-power sample, then simulates the remaining datum distance with the trustworthy measured power.
- **Crr stays 0.0014 (drum-measured, gate-mandated); track "draggability" belongs in the venue `surfaceFactor`.** The owner's field model inflates Crr to 0.0021 to absorb corner rolling; this engine adds that corner rolling explicitly via `kN`, so it keeps 0.0014 and must not also inflate Crr (that would re-introduce a double-count).
- **Geometry fit (§4.8) is approximate.** It recovers a consistent ~19.5 m across both rides (vs the 23 m UCI placeholder), but the rider's own cornering speed modulation confounds the raw `kV` amplitude, so only the shape/phase is reliable. Adequate as a first-pass; not gated.
- **Detection-confirm UI is SVG, not Plotly.** §2 mandates a single Plotly chart wrapper; that lands in P4 with the ride-detail charts. P3's detection screen needs only a line + two drag handles, so SVG keeps P3 dependency-free.
- **Tooling:** added `fit-file-parser`; added `node` to `tsconfig.app.json` types so the co-located Node-based fixture tests type-check (engine runtime purity is unaffected and separately grep-verified).

---

## 2026-07-03 — P3 review pass

**Model:** Claude (Fable 5), via Claude Code CLI

Owner-requested audit of the P3 commit (`a8c340c`) against SPEC §4 physics, §7 gate integrity, data safety, and mobile. **Findings: no severity-1 issues (no wrong physics beyond the already-approved deviations, no data-loss paths); gate tolerances verified 1:1 against §7 — none weakened** (tests actually add tighter un-specced assertions: exact record counts, strict 1 Hz spacing). Two severity-2 items found and fixed:

1. **Deviations that P3 implemented but failed to list (§9 requires explicit listing) — recorded here for the permanent record:**
   - **§4.7.3 oscillation check not implemented** (band-passed speed peaks, two-peaks-per-lap check, peak-phase-in-degrees / `peakSpeedPhaseDeg`). Deferred; needed by P4's speed-vs-position overlay and would also enable a calibrated pre-official duration estimate (the gate-2 residuals of −1.20/+1.07 s are almost entirely the uncalibrated raw-4000 m finish crossing).
   - **§4.7.4 interior-c anchors deviate:** spec anchors the 14-lap window on "same-phase oscillation anchors"; implementation uses official-time-anchored lap-line boundaries. The spec's preference clause covers official *splits*, which we don't have (official *total* only) — so this is a real deviation, chosen because official anchoring is more robust than oscillation on these files. Gate 3 still measures a genuine interior 14-lap `c`.
   - **§4.5.5 partially implemented:** the cross-check (±2.5 s) and the effective alignment (distance-anchoring via `calibrationRace`) exist; the ">1 % stretch → quality flag" does not (no quality-flag system until §4.16, P4). Fixture stretches are 0.49 %/0.43 %, under the threshold.
   - **§4.6 display-only synthesized start power profile not implemented** (energy-side reconstruction is done); it's a P4 ride-detail display feature.
   - **§4.7.2 half-lap boundaries not constructed** (needed for §5.9 fastest-half-lap records; P4/P7).
2. **`extrapolateStart` hardening (`detect.ts`):** unguarded `v[start..start+4]` could read out of bounds on a degenerate file (effort ending within 5 samples of segment end) and propagate NaN through the whole analysis; a decelerating fit (slope ≤ 0) could "refine" t0 *forward*. Now returns no-refinement (0) in both cases, and the index-coincidence `ys[x]` loop is rewritten with explicit indices. Fixture outputs unchanged (both fixtures take the slope>0, dt<0 path).

Severity-3/4 items fixed opportunistically: SpeedTrace drag-handle grab area doubled (16→32 viewBox units — was ~8 CSS px on a phone, unusable by touch); dead `officialTimeS` prop removed from SpeedTrace; odd defensive ternary removed in DetectionConfirm.

Severity-3 items **deferred, on the record:** (a) `buildTimeline` selects the race segment by record count — a long gap-free warmup would win instead of the race; P4's historical-file imports should switch selection to "segment containing a valid §4.5 window"; (b) `fit.ts` `force:true` parses CRC-damaged files silently — wire into §4.16 quality flags in P4; (c) final's negative avg line height (−0.17 m) is physically impossible and encodes rollout/track-length error — P4 UI must label it as calibration residual, not hide it.

**Test status after fixes:** full suite re-run — all tests passing; `tsc -b`, `npm run build`, `npm run lint` clean. Fixture gate numbers identical to the P3 entry (fixes are guards/UI-only).

---

## 2026-07-04 — P4 Rides

**Model:** Claude (Sonnet 5), via Claude Code CLI

**Completed:** the full upload flow (drop → confirm → metadata → save & analyze), CSV import wizard, rides list, and ride detail with every §5.1 chart. Live-verified end-to-end in a real browser (not just build/test-level) by uploading the quali fixture through the actual UI, filling in real metadata, and confirming every chart renders — see the worked numbers below.

**Engine additions (`src/engine/`), needed by the UI but not yet built in P2/P3:**
- Cadence threaded into the ingest `Timeline` (was parsed but discarded).
- `cdaRolling` (§4.9): centered ~1-lap window, step ¼ lap, over a distance-carrying sample series (`raceSampleSeries`, new in `laps.ts`).
- `computeAccelDecel` (`accel.ts`), `lapSpeedVsPositionSeries` (`overlay.ts`), `peakSpeedPhaseDeg` (extends `geometry.ts`'s profile function with a lap-range parameter) — §4.15/§4.7.3.
- `assessQuality` (`quality.ts`, §4.16): the 0–100 badge score with a documented deduction rubric (spec names 7 criteria, not point values).
- `report.ts`: `analyzeRideFull()` wraps the gated `analyzeRide()` with all of the above into `FullRideAnalysis` — the compact §4.15 `AnalysisResult` (persisted) plus the rich per-second diagnostics ride detail needs (timeline, overlay, rolling CdA, a continuous W′bal curve) that are **not** persisted.
- `ENGINE_VERSION` left at `0.3.0-p3` — nothing here changes an existing physics formula, only orchestrates already-tested pieces into new aggregates.

**Real bug found and fixed while building the lap table:** `constructLaps`'s per-lap line-height loop used `interpAt(t, d, lapBoundaryTimes[0])` for lap 1's start boundary, but for a `missingStart` race that boundary (`t0`) is extrapolated *before* the timeline's first real sample — `interpAt` silently clamped to the wrong distance, producing a line height of **−2.24 m** (should be centimeters). Fixed to use the already-correct triangular-extrapolated `detection.d0` for that one boundary; lap 1 now reads 0.162 m, consistent with the rest of the race. Regression test added (`ingest.test.ts`). CdA/gate numbers are unaffected — this bug was isolated to the line-height diagnostic.

**Store additions:** `Settings.cpW`/`wPrimeJ` (generic elite-endurance defaults, manually tuned in the UI — see deviations); `Ride.analysis` typed as the engine's `AnalysisResult` (was `unknown`); `Ride.manualAvgPowerW` for CSV-imported rides with no fit file; `resolveRideDensity`, `analyzeStoredRide`, `bytesToBase64`/`base64ToBytes` helpers.

**UI:** `Chart.tsx` (Plotly wrapper, §2 — binds `react-plotly.js`'s dependency-injectable factory to `plotly.js-dist-min` rather than the full `plotly.js` package); `DetectionConfirm` now hands `{fitBytes, officialTimeS}` up via `onConfirm` instead of dead-ending; `MetadataForm` + `UploadFlow` (venue/gear/density-or-T-P-RH/kit/notes/flags → `analyzeRideFull` → save → navigate); `RidesList` (sortable table, normalized time, avg W, CdA, quality badge); `RideDetail/` (traces, lap table, per-lap CdA + trendline, rolling CdA, start panel, W′bal curve, accel/decel, speed-vs-position overlay, quality panel — 8 sub-components + `index.tsx` orchestrator); `CsvImport.tsx` (paste/upload → parse → column-mapping → preview with per-row validation → create rides without fit files, §3.6).

**Live verification (real browser, not just build/tsc):** uploaded the quali fixture through the actual drop zone, confirmed detection (−1.20 s vs official, matches gate 2 exactly), filled in venue/density/mass, saved, and inspected the persisted `Ride` directly in IndexedDB — `cdaRace=0.16940`, `ci=0.00756`, `qualityScore=94.4`, `startEnergyJ=5768.3`, all **exact matches** to independent Node-script probes of the same pipeline. Rides list showed correct normalized-time scaling (246.793 s → 248.829 s for the denser reference density). Ride detail rendered all nine chart sections with real data (peak power 1342 W and accel/decel totals 104 s/99 s also exact-matched the probes); toggling the Speed checkbox correctly hid/showed that trace. CSV import: parsed a 3-row paste (including a quoted comma field), correctly matched two venues, correctly flagged an unmatched venue for skip-with-reason, and created exactly the 2 valid rides (cleaned up after verification). Checked responsiveness at both desktop (1400 px) and mobile (375 px) — Plotly's `useResizeHandler` adapts correctly.

**Test status:** `npm test` **161/161 passing** (16 files; 25 new P4 tests); `tsc -b`, `npm run build`, `npm run lint` all clean. Engine purity re-verified (grep): only `fit-file-parser` is a non-relative import in real `src/engine` code.

**What's next:** P5 Compare + progression — pick 2+ rides for gap/lap-split/CdA/W′bal/speed-vs-position comparison charts, and a progression view of any metric vs date across all rides.

**Deviations / documented judgment calls (SPEC §9):**
- **Line-height is nearly non-discriminating across laps (newly discovered, UI made honest about it rather than silently hidden).** Lap boundaries are placed at `n·L/calibrationRace` — a single global factor — so the raw distance between any two consecutive boundaries is forced to the same value by construction, regardless of how the rider actually rode. Verified directly: spread across all 16 laps was `0.00000` to 5 decimals. This isn't a new bug (P3's calibration architecture is unchanged and still gate-3-correct) — it's an inherent consequence of not having lap boundaries anchored independently of that calibration, i.e. the still-deferred §4.7.3 oscillation-peak detection (flagged in the P3 review, and again here since it's now visibly a UI concern). `LapTable` shows a caption explaining this when the spread is below 1 mm, rather than presenting a misleadingly-precise-looking column. This is also what closes out the P3 review's item (c): final's −0.17 m is a genuine calibration residual, now labeled as such rather than silently displayed.
- **CP/W′ (§4.13/§5.10) is a manually-set Settings default (400 W / 25 kJ), not a fit.** `estimateCpWprime` (built in P2) needs mean-maximal-power points at several *different* durations; the only data on file is ~4-minute pursuit efforts (one duration), so the fit is underdetermined. The W′bal chart is a genuine computation from the ride's real power series — only the CP/W′ *inputs* are a placeholder the owner should tune in Settings.
- **`Ride.analysis` stays a compact cache; ride detail always recomputes fresh from `fitFileB64`.** Per §3.3's "recomputed on demand," `RideDetail` re-runs `analyzeRideFull()` using *current* Settings/Venue every time it's opened (so a Crr or CP/W′ change is reflected immediately), rather than trusting the cached summary. Deliberately **not auto-persisted on every view** — bumping `updatedAt` just from looking at a ride could interact badly with last-write-wins sync (P1's venue-sync incident was exactly this class of bug). An explicit "Save recomputed analysis" button updates the cache (and hence the rides list) only on a genuine user action.
- **CSV import (§3.6) maps date/venue/gear/finish-time/avg-power/notes plus an ordered set of per-lap-split columns; "1k/2k/3k cumulative" is not mapped or stored** — the `Ride` schema has no field for cumulative split marks, only per-lap `officialSplits`, and adding one wasn't requested. Venue matching is exact-name (case-insensitive); an unmatched venue skips that row with a reported reason rather than fabricating a venue reference. "Avg power" for a fit-less CSV row has nowhere to live in engine-derived data, so it's stored in a new `manualAvgPowerW` field that `RidesList` falls back to only when `ride.analysis` is absent.
- **accelDecel classification (§4.15) uses a ±0.02 m/s per-second deadband** to avoid classifying measurement noise on a steady effort as accel/decel — spec names the fields, not a criterion.
- **Detection-confirm drag handles remain visual-only (carried over from P3, now more visible since the UI is complete).** `detectRace()` doesn't accept a manual start/finish override; the corrective path for a mis-detected race is adjusting the `officialTimeS` field (which the ±2.5 s check and the whole-race calibration key off), not repositioning the handles. Both fixtures auto-detect correctly, so this doesn't block the phase, but a genuinely bad auto-detection on a future ride would need this to become real.
- **Bundle size:** now 5.7 MB (1.7 MB gzip), up from ~1 MB, now that Plotly is actually imported by live routes instead of tree-shaken. Flagged as a P8-polish candidate (dynamic `import()` / code-splitting per Vite's own warning), not addressed now — functionality over bundle size at this stage for a single-owner tool.
- **Tooling:** added `plotly.js-dist-min` + `react-plotly.js` (bound via its `/factory` entry point, not the default export, which hardcodes an import of the full `plotly.js` package) + `@types/plotly.js`; added a `src/types/plotly-dist-min.d.ts` ambient declaration since the dist-min package ships no types of its own. Added `.claude/launch.json` for the dev-server preview tool (portable Node needed the direct `node <vite.js>` invocation, not `npm run dev`, to resolve in this sandbox).

---

## 2026-07-04 — P4 review pass

**Model:** Claude (Fable 5), via Claude Code CLI

Owner-requested audit of the P4 commits (`5cc772d`, `076d112`) against SPEC §4 physics, §7 gate integrity, data safety, and mobile. **Findings: no severity-1 issues (no data-loss paths; backup export covers all five collections with full docs; no new delete paths); §7 gate files untouched by P4 — no weakened tolerances** (verified via commit stats: `fixtures.test.ts`/`expected.json` unchanged; new tests only add assertions). Mobile live-verified at 375 px during P4; no breakage. Two severity-2 items found and fixed:

1. **Missing migration for the new `Settings.cpW`/`wPrimeJ` fields → NaN persisted into `Ride.analysis` on existing devices.** `ensureSeeded()` only seeds when no settings doc exists; the owner's real devices carry a P1-era doc *without* the new fields, so `settings.cpW` is `undefined` at runtime, flows into `wPrimeBalance({cp: undefined})`, and produces NaN through every lap's `wPrimeEnd` and the W′bal curve — **saved into the analysis cache** on the next upload. (P4's live verification missed it because the preview browser seeded fresh with the new defaults.) Fix: `withSettingsDefaults()` in `store/types.ts` — a **read-time** backfill (`{...DEFAULT_SETTINGS_VALUES, ...doc}`) applied at every consumer (`GlobalParams`, `MetadataForm`, `analyzeStoredRide`, `RidesList`). Deliberately *not* a write-time migration: writing the backfilled doc would bump `updatedAt`, and a stale device migrating late could clobber a genuine newer edit via last-write-wins — the exact class of bug behind the P1 venue-sync incident. Unit tests simulate a P1-era doc (backfill works, owner edits always win).
2. **§4.16 scored dropout over the whole timeline segment, not "in race".** `report.ts` passed `timeline.dropoutSeconds` (segment-wide) to `assessQuality`; the final fixture's ~23 interpolated seconds are almost entirely standing-at-the-gate time *before* the start, wrongly docking a clean race ~18 points (73.9/yellow). Fixed to count interpolated samples inside the detected race window only. Final fixture: **73.9/yellow → 94.4/green** (its one genuine in-race interpolated second docks −0.5); quali unchanged at 94.4; CdA values byte-identical (0.1694/0.1676 — scoring only, no physics change). `timeline.dropoutSeconds` remains the segment-wide stat gate 1 asserts on. Regression test added.

Severity-4 items fixed opportunistically: the 700 KB guard error message said "it won't be attached" when the save actually aborts entirely (now says so); `SpeedTrace` power axis gets a 1 W floor so a degenerate all-zero power series can't produce NaN polyline points.

Severity-3 items **deferred, on the record:** (a) `peakSpeedPhaseDeg` averages band-passed profiles over laps 1–16 — the standing-start ramp contaminates the average (quali reports 180°, final 50°, same rider/venue; the steady 3–15 window would be more physical). Changing it alters stored analysis values, so it should ride along with the next `ENGINE_VERSION` bump rather than churn now. (b) Detection-confirm prefills "Official time" with the *detected* duration, so the "+0.00s vs official" chip is initially a tautology — enter the real official time before confirming. (c) Rides-list normalized time applies the steady cube-root scale to the whole time with no §4.12 lap-1 blend — matches the owner's historical whole-time convention, documented in `density.ts`, but is a literal §4.12 deviation now user-visible. (d) `ENGINE_VERSION` was not bumped for P4's line-height output fix; no stale cache exists today (the only saved ride post-dates the fix), but the next output-affecting change must bump it — and should fold in (a).

**Test status after fixes:** `npm test` **165/165 passing** (17 files; 4 new regression tests); `tsc -b`, `npm run build`, `npm run lint` all clean. Fixture CdA/gate numbers identical (fixes are scoring/migration/UI-copy only).

---

## 2026-07-04 — P5 Compare + progression

**Model:** Claude (Sonnet 5), via Claude Code CLI

**Completed:** the full §5.2 Compare page — ride picker, gap chart, lap-split grouped bars, per-lap CdA overlay, W′bal overlay, speed-vs-position overlay, and the progression view — plus the pure comparison math it's built on.

- `src/pages/Compare/compare.ts` — pure, unit-tested: `buildDistanceTimeSeries`/`timeAtDistance` (inverts each ride's 1 Hz cumulative-distance-vs-time series so the gap chart can sample every ride at a common distance grid, since different rides' lap boundaries land at different distances even at the same venue), `gapCharts` (first selection = reference, matching §5.2's spec exactly), `speedPositionAverage` (bins each ride's per-lap speed-vs-position points, already computed by `report.ts`'s `overlay`, across its steady laps into one representative line per ride — the per-ride, per-lap version from RideDetail would be 16 lines × N rides and unreadable here).
- `RideSelector.tsx` — ordered multi-select (an array, not a Set: check order determines the gap-chart reference, shown live as "Reference"/"#2"/etc.), color-coded dots shared with every chart via `colorFor`.
- `GapChart.tsx`, `LapSplitChart.tsx`, `CdaOverlayChart.tsx`, `WBalOverlayChart.tsx`, `SpeedPositionOverlayChart.tsx` — one ride/series per selected ride, reusing the same `analyzeStoredRide` → `FullRideAnalysis` pipeline RideDetail uses (so Compare always reflects current Settings/Venue, same "recomputed on demand" contract as §3.3).
- `Progression.tsx` — metric selector (finish time, normalized time, CdA, avg power, start time, avg line height) vs date across **all** rides (not just selected), kit tags on hover, outdoor rides (venue.indoor===false) rendered as hollow markers via a transparent-fill/colored-stroke marker, trendline (`linearTrend`, reused from `RideDetail/trend.ts`) excluding outdoor rides by default with a toggle to include them.
- `src/pages/Compare/index.tsx` — orchestrates selection → per-ride analysis (with a visible per-ride error banner for a missing venue or fit file) → the five compare charts (needs 2+ analyzable rides) → Progression (always visible once any ride exists, independent of selection).

**Real bug found and fixed during browser verification:** Plotly's date axis, given two rides sharing the exact same `date` string, computes a zero-width auto-range and ticks it in **sub-second** increments (`23:59:59.999`, `00:00:00.0005`) — unreadable, and this isn't a contrived case: a qualifying + final on the same day is the normal case for this owner's data. Fixed by pinning `xaxis: { type: 'date', tickformat: '%Y-%m-%d' }` on the Progression chart.

**Live verification (real browser via the preview tool, not just build/tsc):** loaded both fixtures (the quali ride was already saved from a prior P4 session; uploaded the final fixture through the actual drop-zone → detection-confirm → metadata-form flow, simulating the file drop with a real `DataTransfer`/`File` since no OS file-picker is available in this environment — bytes fetched from a fixture copy temporarily placed in `public/` and removed immediately after, never committed). Selected both rides in Compare and confirmed all five charts render with physically sensible shapes: the gap chart shows the final (slower, 248.699 s) losing a steady ~1.5–2.3 s to the quali (246.793 s) reference across the whole 4000 m, matching the ~1.9 s official gap; the CdA overlay lands both rides' per-lap values inside [0.16, 0.19], consistent with the gate-4 race-window numbers (0.1694/0.1676); the speed-vs-position overlay shows the expected two-peaks-per-lap oscillation from the two-bend/two-straight geometry. Checked responsiveness at 375 px (mobile) and native desktop width — Plotly's `useResizeHandler` and the ride-selector table's horizontal scroll both work; no console or server errors at any point. `npm test` **170/170 passing** (18 files; 5 new Compare tests); `tsc -b`, `npm run build`, `npm run lint` all clean.

**What's next:** P6 Scenario tools — Adjuster, Watts to Win (+ event records CRUD), Gains, solve-for-anything, isochrone chart.

**Deviations / documented judgment calls (SPEC §9):**
- **Pinned scenarios are not selectable in Compare yet.** §5.2 says "pick 2+ rides (and/or pinned scenarios)," but Scenario CRUD is P6 (Adjuster) — no scenario can exist with a `result` today, so there's nothing to compare. `CompareItem` (the shape every chart consumes) is ride-agnostic (`{id, label, color, full}`), so wiring in scenario-derived items later shouldn't require touching the chart components.
- **Speed-vs-position overlay averages laps 3 → min(15, lapCount−1) per ride**, matching the steady-window convention already used for CdA (`CRUISE_FIRST_LAP`) and the geometry fit (`STEADY_LAST_LAP`) rather than inventing a new range. Binned (40 bins/lap) rather than raw per-second points, since different rides' laps don't share position samples to overlay directly.
- **Gap-chart resolution is a 20 m grid, not per-sample.** The distance→elapsed-time inversion is linear between each ride's own 1 Hz samples, so 20 m is well inside the interpolation error tolerance (a rider covers ~20 m in little more than 1 sample already) while keeping the chart light.
- **Progression's "start time" metric is `startMetrics.timeTo95PctCruise`** (already computed by `report.ts`) and "avg line height" is the mean of `analysis.laps[].lineHeightM` — §5.2 names both metrics but not their derivation; both reuse fields the P4 ride-detail page already established rather than adding new engine surface.
- **Progression reads only the persisted `ride.analysis`** (not a live recompute per ride), matching `RidesList`'s existing pattern — a ride with no cached analysis (e.g. CSV-imported, fit-less) simply doesn't contribute a point for CdA/avg-power/start-time/line-height, though it still can for finish/normalized time.

---

## 2026-07-04 — P6 Scenario tools

**Model:** Claude (Sonnet 5), via Claude Code CLI

**Completed:** the full §5.3–5.5/§4.11 scenario stack — Adjuster (baseline + overrides, live predicted time/lap-splits/overlay-vs-baseline, save/pin/load/delete scenarios), Watts to Win (event CRUD, per-event watts/ΔCdA-to-match + "time at +N W" table), Gains (tornado chart + table with a seconds ⇄ watts-equivalent toggle, isochrone chart), and solve-for-anything — all built on one shared physics module rather than five separate ad-hoc implementations.

- `src/store/scenario.ts` — the core: `resolveScenario(baseline, overrides, settings, venues)` turns a baseline (a ride or `'blank'`) plus SPEC §3.4 overrides into everything `simulate()`/`solve()` need; `runScenario()` runs the forward sim; `solveScenarioUnknown(key, targetTimeS, resolved)` bisects for any one of {power, CdA, crr, mass, ρ} (§4.11) by delegating straight to the already-gated `solve*ForTime` functions from P2; `wattsEquivalentForTimeGain()` backs the Gains unit toggle; `resolveScenarioBaseline()` turns a persisted `Scenario.baseline` (rideId | 'blank') back into a runnable baseline, shared by Adjuster (loading a saved scenario) and Compare (running a pinned one).
- **`scenarioToFullAnalysis()`** — the Compare integration: converts a scenario's simulated trajectory into the same `FullRideAnalysis` shape a real ride produces (synthetic `Timeline`/`LapConstruction`/`Detection`, reusing `lapSpeedVsPositionSeries`, `computeAccidentDecel`, `wPrimeBalance`, `cdaRolling` directly since a scenario's `calibrationInterior=1`/`d0=0`), so **pinned scenarios flow through all five existing Compare charts with zero chart-side branching** — `Compare/index.tsx` and a new `PinnedScenarioSelector.tsx` just add `scenario:<id>` alongside `ride:<id>` in the same ordered selection array `RideSelector.tsx` already used.
- `src/pages/Adjuster/` — `OverrideForm.tsx` (baseline picker + CdA/Crr/mass/density/venue/gear + a power-mode toggle: scale the ride's real recorded pacing by a % vs. replace it with a flat constant-W target), `ResultPanel.tsx` (predicted time, Δ vs. unmodified baseline, a gap-chart-style overlay reusing Compare's `buildDistanceTimeSeries`/`gapCharts`, lap-split bars), `SolveForAnything.tsx` (pick the unknown + a target time → solved value → "Apply to override"), `SavedScenarios.tsx` (load/pin/delete).
- `src/pages/WattsToWin/` — `EventForm.tsx` (§3.5 CRUD: name/date/venue, winners per round, linked rides), `EventRecordsTable.tsx` (per ride × winner: gap, watts-to-match and ΔCdA-to-match via `solveScenarioUnknown`, and a "+10/20/30 W" mini-table via a `powerScale` override sized to add exactly N watts while preserving the ride's real pacing shape).
- `src/pages/Gains/gains.ts` — `computeGainsRows()`: the five §5.5 fixed perturbations (−0.005/−0.010 CdA, −0.0002 Crr, −1 kg, −0.02 ρ, +10 W) plus their watts-equivalents, sorted by magnitude for the tornado chart; `buildIsochroneGrid()`: a CdA×power contour grid (range auto-widened to include every analyzed ride's own point) via direct `simulate()` calls at constant power.

**Two real bugs found and fixed during live browser verification (not caught by unit tests until added after the fact):**
1. **Scenarios built from a real ride's recorded power ran ~5–6 s slow** (verified on the quali fixture: baseline scenario predicted 252.15 s vs. official 246.79 s — a 5.4 s systematic error, visible in Compare's gap chart as the scenario looking *behind* the real ride for most of the race before narrowing at the very end). Root cause: the SRM under-reads power while the rider is still accelerating off the gate — exactly the problem `reproduceTime` (P3, gate 5) already solves by skipping ahead to the first trustworthy power sample and starting the sim from there with the real elapsed time/distance/speed "in the bank." `resolveScenario` didn't do this — it fed the ride's raw recorded power into `simulate()` from a plain v0=0.5 standing start. Fixed: `resolveScenario` now computes the same head-start skip (`headStartS`/`v0`/`distanceM`) as `reproduceTime`, and `runScenario` prepends `headStartS` back onto every reported time. Post-fix, the quali baseline scenario predicts 245.73 s (−1.06 s vs. official — in the same ballpark as gate 5's −0.85 s), and the −0.010 CdA scenario is **4.26 s faster**, comfortably inside the "roughly 2–4 s" DoD band. A constant-power override (no real schedule in play) is unaffected and still starts from the plain §4.10 standing start.
2. **`simulate()`'s lap-line crossings silently dropped the 16th lap** once distanceM stopped being an exact multiple of 250 m (a direct consequence of fix #1 truncating distanceM by the head-start's datum) — `nextLapDist` starts at a fixed `L` and increments by `L`, so if the truncated `distanceM` falls a few meters short of `16×250`, the 16th target is never reached and only 15 splits get pushed. This wasn't just a display glitch: the synthetic Compare-adapter timeline's distance array also wasn't shifted by that same datum, so a scenario's `buildDistanceTimeSeries` topped out ~250 m short of the real finish — the visible symptom was a bizarre 18 s cliff at the very end of the gap chart. Fixed with a new, backward-compatible, opt-in `SimInput.lapPhaseOffsetM` (default 0, so every existing gate/test is untouched): lap targets are placed at `n·L − lapPhaseOffsetM`, and `resolveScenario` sets it to the head-start's datum. `scenarioToFullAnalysis`'s synthetic distance array is shifted by the same amount. Post-fix, the gap chart is a clean, monotonically-growing curve (0 → ~4.5 s over the race) instead of a hockey-stick; both regressions are now asserted directly (`sim.lapSplits.length === 16`, `buildDistanceTimeSeries(...).distM` reaching >3950 m).

**Live verification (real browser via the preview tool):** created the scenario "quali −0.010 CdA" from the real quali ride baseline — predicted 241.678 s vs. an unmodified-baseline prediction of 245.73 s (Δ **−4.26 s**, matching the automated test and the DoD's "roughly 2–4 s" band). Pinned it, and confirmed it appears in Compare's picker and renders correctly in all five charts (gap chart: clean monotonic curve either direction depending on which is the reference; W′bal overlay: exactly overlapping the real ride's, since CdA doesn't affect power — correct; speed-vs-position overlay: the scenario faster at every position in the lap — correct). Solve-for-anything round-tripped live: set a constant-power scenario to 480 W (predicted 246.374 s), then solved "Power" for target time 246.374 s → **480.0 W exactly**. Built a real "2025 Worlds" event (Charlton 242.844 s qualifying, Ganna 244.122 s final) and confirmed sensible watts/ΔCdA-to-match and +N-W numbers for both linked rides. Gains tornado chart reproduced the same 4.26 s CdA delta and sorted all six perturbations sensibly (CdA changes dominate, as expected at these speeds); isochrone chart rendered correct contours (time rises with CdA, falls with power) with both rides' points plotted near the center. Checked mobile (375 px) on Adjuster/Watts to Win/Gains — all readable, tables scroll horizontally, no console errors anywhere in the session.

**Test status:** `npm test` **182/182 passing** (20 files; 12 new: `store/__tests__/scenario.test.ts` covers the DoD CdA-delta check, the solve round-trips for power/CdA/mass, `wattsEquivalentForTimeGain`, and both regressions above; `pages/Gains/__tests__/gains.test.ts` covers the six perturbations' sign/ordering and the isochrone grid shape); `tsc -b`, `npm run build`, `npm run lint` all clean.

**What's next:** P7 Pacing + Race Day + Calculators UI + Records.

**Deviations / documented judgment calls (SPEC §9):**
- **Gear is stored on a scenario's overrides but doesn't feed the physics.** The engine has no drivetrain model — cadence is a consequence of gear + speed, not an input to `simulate()`. §3.4 lists `gear` among the override keys, so it's captured/displayed/round-tripped through save-and-load, but changing it alone doesn't change a scenario's predicted time. This mirrors §5.8's Cadence calculator, which derives cadence from gear/speed/rollout independently.
- **Watts-equivalent (§5.5's unit toggle) deliberately solves against a *flat constant power* baseline, not the ride's real recorded pacing schedule** — verified directly: applying the "+10 W" perturbation (which scales the real schedule, preserving its shape) to the quali ride and asking "what's the watts-equivalent of that time gain" returns ~18 W, not 10 W. This is not a bug: a non-flat pacing schedule at a given average power is slower than flat pacing at the same average (aero drag is convex in speed, so speed variance costs time), so "the flat power that reproduces a schedule-based time gain" is a genuinely different number from the schedule's own power delta. The round trip *is* exact (verified in both `scenario.test.ts` and `gains.test.ts`) when the baseline itself is already flat power — i.e. a `'blank'` baseline, or any scenario using the "constant target (W)" power mode.
- **A constant-power override on a ride baseline keeps that ride's real head start** (`headStartS`/`v0`/`distanceM` from the actual recording), rather than resetting to a fresh v0=0.5 standing start — the head start is a historical fact about how long the rider actually took to get up to trustworthy-power-reading speed, independent of what hypothetical power model is applied to the rest of the race. Only a `'blank'` baseline (no recording at all) uses the plain §4.10 default.
- **Per-lap CdA for a scenario is set directly to the input `cdaM2`**, not re-derived via `energyBalanceCda` — a scenario's CdA is exactly known (it's what was simulated with), so re-fitting it from the synthetic energy balance would just reintroduce small Euler-integration noise for no benefit. `analysisResult.ci` is reported as 0 for the same reason.
- **The isochrone grid always uses a constant (flat) power axis**, independent of any baseline's real pacing schedule — the chart's purpose is comparing flat average-power/CdA combinations against each other and against rides' own season-average points, not modeling any one ride's actual pacing.
- **`EventRecordsTable`'s "time at +N W" and Gains' "+10 W" row both use the `powerScale` lever** (scale factor sized so the average shifts by exactly N W while the real pacing shape is preserved) rather than a flat constant-power override, for consistency with the Adjuster's own "scale real pacing" mode and because a flat-power answer would suffer the same schedule-vs-flat divergence noted above.
- **Bundle size** now 5.79 MB (1.73 MB gzip) — negligible growth from P5 (five more pages sharing the same Plotly `Chart` wrapper); still flagged as a P8 code-splitting candidate, not addressed now.

---

## 2026-07-04 — P7 Pacing + Race Day + Calculators UI + Records

**Model:** Claude (Sonnet 5), via Claude Code CLI

**Completed:** the four remaining §5.6–§5.9 pages, built on the already-tested P2/P3/P6 engine surface plus two small, deliberately narrow engine additions.

- **Engine additions:** `constructHalfLaps`/`halfLapTimes` (`engine/ingest/laps.ts`, §4.7.2) — half-lap boundary crossings at datum n·125 m, mirroring the existing full-lap crossings at 2× resolution off the same `calibrationRace`/`d0`; deferred at P3 and flagged again at the P3/P4 reviews as needed for §5.9's fastest-half-lap record, built now that Records needs it. `solveTemplatePowerForTime` (`engine/solve.ts`) — bisects the steady-power level for the owner-shaped standing-start template (`defaultStartPower`) to hit a target time, extending §4.11's flat-power solver to the template shape; shared by Pacing's ghost builder and Race Day's required-schedule output.
- **Store addition:** `Settings.gearInventory` (`{chainring,cog}[]`, seeded 59×14/60×14/64×15/65×15 per §5.8), backfilled for pre-P7 docs via the existing `withSettingsDefaults` read-time-default pattern (no write-time migration, same last-write-wins safety reasoning as P4's cpW/wPrimeJ backfill).
- **§5.9 Records** (`pages/Records/records.ts` + `index.tsx`): fastest lap/half-lap/first-lap, fastest 1k/2k/3k/4k (cumulative sums of the persisted per-lap breakdown — no new engine surface needed beyond half-laps), best CdA, best normalized time (reuses `equivalentTimeAtRefDensity`, works even for a fit-less CSV ride), best line quality (smallest **|avgLineHeightM|**, not smallest signed value — a negative line height is a calibration artifact per the P3 review, not literally "better"). Recomputes each ride fresh via `analyzeStoredRide` (needed for half-lap data, which isn't part of the compact persisted `AnalysisResult`) with a fallback to the cached `ride.analysis` on failure or a fit-less ride, so a CSV-imported ride still contributes what it can rather than being dropped entirely. Indoor-only filter, default on.
- **§5.6 Pacing** (`pages/Pacing/pacing.ts` + `GhostBuilder.tsx`/`OptimalityPanel.tsx`/`index.tsx`): an "environment" baseline picker (ride or blank, reusing `resolveScenario` for CdA/ρ/mass/track — deliberately ignoring the baseline's own recorded power/head-start, since the ghost is a hypothetical plan from a clean standing start). Ghost builder: target time → even (flat) or owner-shaped-template schedule via `solveGhostSchedule`, optionally overlaid against any real ride's gap curve (reuses Compare's `buildDistanceTimeSeries`/`gapCharts` directly — cross-page import, same precedent as Adjuster→Compare in P6). Optimality panel: `pacingOptimality` wires a real ride baseline's actual recorded pacing (`resolved.power` with no override *is* the ride's real schedule, per `resolveScenario`) into P2's `comparePacing`, with an editable CP/W′ panel (local what-if, defaults from Settings, not persisted — tune permanently in Settings).
- **§5.7 Race Day** (`pages/RaceDay/raceday.ts` + `index.tsx`): venue + T/P/RH-or-direct-density + gear + goal (target time *or* target power) → `computeRaceDayPlan()` → required steady power, predicted time, 16-lap schedule, per-lap cadence (`cadenceRpm`, P2). Big-type card layout for the headline numbers. "Save as scenario" persists a `'blank'`-baseline `Scenario` with `avgPowerW` set to the plan's steady power — explicitly **not** the template shape (Scenario's overrides schema has no template-shape field, so this is a documented flat-power approximation, same divergence class as P6's watts-equivalent note).
- **§5.8 Calculators** (`pages/Calculators/{index,CadenceCalculator,PowerForSpeedCalculator,WattsSavedCalculator,TimeAdjusterCalculator}.tsx`): four tabs (URL-synced via `?tab=`) over the P2 `engine/calculators.ts` math, unchanged since P2. Cadence: editable/persisted gear inventory table × 13.0–17.0 s lap-time grid, venue-aware lap length, highlights the matching gear column when opened via `?chainring=&cog=` (wired from a new "Cadence for N×N" link on Ride Detail). Power-for-speed: P(v) curves per CdA, flat-equation/full-track-model toggle. Watts saved: ΔP grid, speed × counts. Time adjuster: two T/P/RH-or-direct env blocks, fast-mode lap-vector scaling (default) or full-sim mode (re-simulates a real ride's actual power at the target density via a direct `simulate()` call, sharing `resolveScenario` for the ride's CdA/params/track).

**Real bugs caught before they shipped (self-review during implementation, not live verification):**
1. **`GhostBuilder`'s overlay-ride analysis initially reused the *environment* baseline's venue** instead of the overlay ride's own `venueId` — silently wrong physics for any overlay ride at a different venue than the environment ride (e.g. comparing a ghost built at Peñalolén against a real ride from Ballerup would've analyzed the Ballerup ride with Peñalolén's track geometry). Caught before running the app; fixed to look up `venues.find(v => v.id === overlayRide.venueId)`.
2. **`pacingOptimality`'s default settle-power grid, inherited from `optimizePacing` (P2), centers on `cp`** — fine when CP is a real fit, but Settings' CP/W′ is still a generic manually-set placeholder (§4.13 deviation, P4) that sits well below an elite pursuiter's real ~480 W race power. Live-verified on the "2025 Worlds" ride: with the grid centered on the 400 W default, the "optimal" schedule search couldn't even consider the rider's real power level and reported a bogus result slower than the ride's actual pacing (a physically backwards "time lost to pacing" of −1.56 s that should never be that far negative). Fixed by defaulting the settle-power grid to a spread around the **baseline's own average power** instead — what the rider actually demonstrated they can hold for this exact effort — while still honoring an explicit override. Post-fix the same ride's "time lost to pacing" narrows to −1.79 s (a small negative, not a bogus one) — still not ≥0 because the pacing family (3 free parameters, fixed opening/kick structure) and a still-generic CP/W′ aren't guaranteed to strictly dominate a real rider's actual pacing; documented in the test and in-code rather than papered over.

**Live verification (real browser via the preview tool):** all four pages loaded and interacted with against the two real fixture rides already in the store. Records: fastest lap 14.177 s, half-lap 6.730 s, first lap 19.610 s, 1k/2k/3k splits, best CdA 0.1676 m², best normalized time, best line quality 0.162 m — all correctly linked to the right ride, indoor-only filter toggles correctly. Pacing: selected "2025 Worlds" as the environment, ghost builder solved a 245 s owner-shaped schedule (529 W steady) and overlaid it against the real ride (gap chart shows the real ride briefly ahead early, ending ~+3.5 s behind at the finish — consistent with 248.699 s real vs. 245 s target); optimality panel populated as described above. Race Day: VELO Sports Center, ρ=1.15 direct, gear 65×15, CdA defaulted to the best analyzed 0.1676, goal "245 s" → 542 W required, 16-lap schedule with cadence converging ~114.6 rpm; "Save as scenario" persisted correctly and the saved scenario appeared in Adjuster's list with the right predicted time. Calculators: Cadence grid renders correctly with the seeded gear inventory; Power-for-speed curves render for the CdA list; **Watts saved reproduced gate 7b exactly (60 km/h, 5 counts, ρ=1.15 → 13.31 W)**; **Time adjuster fast mode reproduced gate 7c exactly (15.6 s @ ρ 1.1722→0.9934 → 14.7627 s, target 14.7626)**; full-sim mode ran a re-simulation without error. The Ride Detail → Calculators gear-highlight link (`?chainring=65&cog=15`) correctly highlights the 65×15 column. Zero console errors across the entire session.

**Test status:** `npm test` **195/195 passing** (25 files; 13 new: half-lap fixture invariant test, `solveTemplatePowerForTime` round-trip, `Records`/`Pacing`/`RaceDay` glue-module tests); `tsc -b`, `npm run build`, `npm run lint` all clean.

**What's next:** P8 Polish — mobile QA pass on every page, quality badges everywhere, empty states, loading states, error handling, final deploy. (P6's and P7's bundle-size flag is a natural companion task but not part of P8's charter per §8 — raise separately if the owner wants it addressed.)

**Deviations / documented judgment calls (SPEC §9):**
- **Half-lap construction assumes the fixed 250 m/16-lap datum** the rest of `laps.ts` is already hardcoded around (`LAP_M`/`N_LAPS` constants, unchanged since P3) — `buildRideRecordStats` only computes a fastest-half-lap contribution when `venue.lapLengthM === 250`, so a 333 m-track ride (7-Eleven Velodrome) simply doesn't contribute to that one record. This mirrors the pre-existing P3 limitation (the whole ingest pipeline is 250 m-shaped today) rather than introducing a new one.
- **"Best line quality" ranks by smallest `|avgLineHeightM|`, not smallest signed value** — per the P3/P4 reviews, a negative average line height is a calibration-residual artifact (the final fixture's −0.17 m), not a rider genuinely riding under the black line, so ranking on the signed value would let a bigger artifact "win" over a smaller real one.
- **Race Day's CdA default is the minimum `cdaRace` across all analyzed rides** (falling back to 0.19, the same nominal guess `resolveScenario` uses for a blank scenario baseline) rather than the most recent ride's — reasoned as "the best demonstrated aero position," overridable via the CdA field's manual input at any time.
- **"Save as scenario" (Race Day) and the ghost builder (Pacing) both produce flat-power scenarios/comparisons, never a persisted template shape** — `Scenario.overrides` (§3.4) has no field for a start-ramp template, a gap that already existed before P7 (a blank-baseline Adjuster scenario has always been flat-power-only, per `resolveScenario`). Both pages caption this explicitly rather than silently mismatching the displayed plan's own numbers.
- **Pacing's CP/W′ panel is a local, non-persisted what-if** — it defaults from Settings on mount but editing it there doesn't write back; the owner tunes the real default on the Settings page (§5.10), consistent with CP/W′ still being a manually-set placeholder rather than a per-ride fit (§4.13, P4 deviation, unchanged).
- **Calculators' gear inventory writes straight to `Settings.gearInventory` on every edit** (no separate save button) — consistent with `GlobalParams`' write-on-blur convention elsewhere in Settings, even though this particular editor lives on the Calculators page rather than Settings itself (§5.8 places the gear table inside the Cadence calculator, not §5.10).
- **Time adjuster's full-sim mode re-simulates from a fresh standing start** (via a direct `simulate()` call with the ride's `resolved.power`/CdA/params/track but the target ρ substituted), not via the head-start-aware `reproduceTime`/`runScenario` path P6 uses elsewhere — acceptable for the calculator's "what would this ride's pacing look like at a different density" question, which doesn't need gate-5 fixture-reproduction precision, but is a real (if minor) inconsistency with the head-start handling used everywhere else a real ride's power schedule is re-simulated; flagged for a future pass if the owner notices the numbers don't quite match Adjuster's for the same ride.
- **Bundle size** now 5.83 MB (1.74 MB gzip) — negligible further growth from P6; the P8 code-splitting flag stands.

---

## 2026-07-04 — P8 Polish

**Model:** Claude (Sonnet 5), via Claude Code CLI

**Completed:** §8's P8 checklist — audited first (a research pass across all 10 pages, not just this session's own P7 additions), then fixed the concrete gaps it found, then live-verified every fix in a real browser at both mobile (375 px) and desktop widths.

**Audit approach:** rather than guessing at gaps, dispatched a read-only research pass across every `src/pages/*/index.tsx` (and sub-components) checking three things per page: (a) does it show a distinct loading state before `settings`/`rides`/`venues` resolve, vs silently rendering as if there's no data; (b) what happens when the relevant collection is empty; (c) every ride-selector/list/card component, and whether the ride's §4.16 quality badge is visible there. Findings:
- **Loading states: already correct everywhere** — every page already guards on `if (!settings) return <p>Loading…</p>` before rendering (established since P4/P5). No changes needed.
- **Error handling: already correct everywhere** — every `analyzeStoredRide`/`resolveScenario` call site across all pages was already wrapped in try/catch with a rendered error message (RideDetail, Compare, Gains, GhostBuilder, Records, EventRecordsTable). No changes needed.
- **Empty states: two real gaps.** Gains showed nothing but the (empty-looking) baseline selector when no baseline was chosen — no hint that picking one would reveal the tornado/isochrone charts. Race Day's `venue = venues.find(...) ?? venues[0]` degrades to `undefined` with zero venues (an edge case — venues are seeded by default) without any explicit message, unlike Calculators' existing "Add a venue in Settings first" guard for the same condition.
- **Quality badges: genuinely absent from every ride selector/list except the Rides table itself and RideDetail's own QualityPanel** — Compare's ride-picker, Adjuster's/Pacing's/Gains' baseline dropdowns, and Watts to Win's ride checklist/per-ride records all showed a ride's name/date/time but never its §4.16 score, even though the data (`ride.analysis.qualityScore`) was already sitting right there.

**Fixes:**
- `pages/Gains/index.tsx` — added "Pick a baseline above to see the marginal-gains tornado chart and isochrone grid." when nothing is selected yet, plus a quality badge caption under the baseline `<select>` once a real ride is chosen.
- `pages/RaceDay/index.tsx` — added `if (venues.length === 0) return <p>Add a venue in Settings first.</p>`, matching Calculators' exact wording/pattern for the same precondition.
- **Quality badges added** (all reusing the existing `qualityBadgeForScore`/`BADGE_CLASSES` helpers from `pages/Rides/format.ts` — no new badge logic, just wiring the existing one into six more places): `Compare/RideSelector.tsx` (new column in the ride-picker table), `Adjuster/OverrideForm.tsx` and `Pacing/index.tsx` (a small badge caption under the baseline/environment `<select>`, since a native `<select>`'s `<option>` can't hold rich markup — the badge shows once a real ride is the current selection, not inside the dropdown itself), `WattsToWin/EventForm.tsx` ("my rides at this event" checklist) and `WattsToWin/EventRecordsTable.tsx` (per-ride section header), `Records/index.tsx` (next to each record's ride link).
- **Mobile QA pass (375 px, live browser):** walked all 10 tabs. No broken layouts found — every data table already uses the established `overflow-x-auto`/`max-h-*.overflow-y-auto` pattern from P4/P5, form fields stack correctly in a single column, and the bottom tab bar's horizontal scroll (present since P0) still works. Confirmed the new badges render correctly inside the existing scrollable tables (initially looked like a layout bug in a screenshot — the table just hadn't been scrolled right yet — false alarm, no fix needed).

**Live verification (real browser via the preview tool):** reloaded and re-checked every page touched above. Compare: ride-picker table now shows a 94/94 badge for both fixture rides (scrolled into view, confirmed not a layout regression). Adjuster: selecting "2025 Worlds" as baseline shows "Quality 94" under the dropdown. Pacing: same, under the Environment dropdown. Gains: empty state now shows the hint text before any baseline is picked. Watts to Win: both the event-form ride checklist and the per-ride records sections show 94/94 badges. Race Day: confirmed the new venues-guard doesn't affect the normal (8-seeded-venues) path — form and 542 W plan render exactly as before. Records: badges render next to all ten record rows' ride links. Zero console errors throughout.

**Test status:** `npm test` **195/195 passing** (no new tests — this phase is UI-polish over already-tested data, not new engine/pure-module surface); `tsc -b`, `npm run build`, `npm run lint` all clean.

**What's next:** nothing — this was the last planned phase (§8). Two items remain explicitly flagged but out of P8's charter, carried forward for the owner to prioritize if wanted: (1) bundle-size code-splitting (flagged at P6/P7, 5.83 MB / 1.74 MB gzip, single-owner app so not urgent); (2) the Time Adjuster full-sim mode / Adjuster head-start inconsistency noted in the P7 entry above.

**Deviations / documented judgment calls (SPEC §9):**
- **Badges next to native `<select>` baselines are a caption below the dropdown, not inside it.** HTML `<option>` elements are plain-text only — there is no way to render a colored badge inside a native select's option list. Converting Adjuster's/Pacing's/Gains' baseline pickers to a custom (non-native) dropdown component purely to embed a badge per option was judged out of proportion for a polish pass (it would touch the same components' keyboard/accessibility behavior, which isn't broken today); the caption-below approach gets the same information in front of the owner with a one-line change per page instead.
- **GhostBuilder's "overlay vs ride" selector, Gains' "pinned scenario" option group, and TimeAdjusterCalculator's "ride to re-simulate" selector do not get a quality-badge caption** — judged lower-value than the six spots fixed (they're secondary/optional pickers, not the primary baseline), and every one of them already reuses a ride the owner just saw badged elsewhere on the same page (Adjuster/Pacing's primary baseline, or Records/Rides).
- **No changes made to loading or error-handling states** — the audit's conclusion (already correct everywhere) is itself a P8 deliverable, not a gap; re-confirmed by reading every relevant call site rather than assuming the audit's own report was complete.

---

## 2026-07-04 — P5–P8 review pass

**Model:** Claude (Fable 5), via Claude Code CLI

Owner-requested audit of everything since the P4 review (`afcf41c` P5, `be31ca9` P6, `697dffb` P7, `5ea35d2` P8 — none previously reviewed) against SPEC §4 physics, §7 gate integrity, data safety, and mobile.

**§4 physics:** no undocumented formula deviations found. The engine-side changes since P4 are additive and correct: `constructHalfLaps`/`halfLapTimes` reuse the §4.7 calibration with a pair-sum consistency test; `lapPhaseOffsetM` (simulate.ts) is opt-in with default 0, so every §4.10 gate path is untouched; `solveTemplatePowerForTime` is a straight §4.11 bisection over the existing template. `resolveScenario`'s head-start skip was verified line-for-line equivalent to `reproduceTime` (analyze.ts, gate 5) — same `MIN_VALID_POWER_W=100` walk, same `c·v`/`c·(d−d0)` datum conversion. The deviations that do exist (watts-equivalent solved against a flat baseline, Race Day/ghost flat-power scenario saves, time-adjuster full-sim fresh start, §4.14 settleW grid re-anchored on baseline avg power) were all already explicitly listed in the P5–P8 entries per §9 — nothing implemented-but-unlisted.

**§7 gates:** `fixtures.test.ts` and `data/fixtures/expected.json` are byte-identical since the P3 review verified them 1:1 against §7 (confirmed via `git diff 585f59e..HEAD` — no diff). Tolerances in `expected.json` re-checked against §7 directly: 2.5 s detection, [0.99,1.01] calibration, 16 laps, [0.16,0.26] CdA, 0.015 m² agreement, ±1.5 s reproduction — exact. New tests only add assertions (half-lap pair-sum, template-solver round-trip, two P6 regressions).

**Data safety:** backup export covers all five collections with full docs (`gearInventory` rides along inside settings); `withSettingsDefaults` read-time backfill correctly extended to `gearInventory` (no write-time migration — same last-write-wins reasoning as P4). **One severity-2 found and fixed: unguarded destructive deletes.** Saved-scenario delete (`Adjuster/index.tsx`) and event delete (`WattsToWin/index.tsx`) fired on a single click/tap with no confirmation and no undo — on a signed-out or local-only device that permanently destroys hand-entered data (an event's winners list, a saved scenario), and on mobile the Delete button sits right next to Load/Edit. Both now `window.confirm` with the item's name first. Live-verified in the browser (confirm stubbed to refuse → correct message shown, data intact; zero console errors).

Severity-3 fixed opportunistically: Records' cumulative 1k/2k/3k marks summed the first 4/8/12 laps of *any* analyzed ride, which equates laps with round distances only on a 250 m track — now guarded on `venue.lapLengthM === 250`, matching the file's own half-lap guard (latent today: the ingest pipeline is 250 m-only, so no current data changes).

Severity-4 noted, not fixed: (a) a pinned scenario's gap-chart series starts at its head-start datum (~50–60 m), so the first two-ish grid points clamp to t=0 — cosmetic, invisible at chart scale; (b) clearing a gear-inventory input persists 0 (Number('')===0) and renders a degenerate cadence column until retyped; (c) P1's no-tombstone sync gap now has more surface (scenario/event deletes don't propagate to Firestore and can resurrect on next sign-in reconcile) — flagged since P1, worth an owner decision if deletes start mattering cross-device.

**Mobile:** no code-level breakage found in the P5–P8 pages (all tables use the established `overflow-x-auto` pattern, forms stack single-column); P8's own documented 375 px live pass covered all 10 tabs, and this session's fix (a native confirm dialog) has no layout surface.

**Test status after fixes:** `npm test` **195/195 passing** (23 files); `npm run build` and `npm run lint` clean. Fixture gate numbers untouched (fixes are UI-guard/record-eligibility only, no physics change).

---

## 2026-07-05 — Owner feedback round 1 (19 items)

**Model:** Claude (Fable 5), via Claude Code CLI

Owner supplied a 19-item backlog plus his "IP Data .xlsx" workbook. Everything below was built, tested, and live-verified in the browser (desktop + narrow mobile width). `npm test` **218/218** (25 files; 23 new tests), `tsc -b`, `npm run build`, `npm run lint` all clean, zero console errors.

**Investigations (items 10/15/19), run as Node probes against the two fixtures:**
- **(10) Avg-power discrepancy (owner sees 499/503, app showed 487/494):** the app's number time-averages over the full official duration, counting the SRM's under-read standing start as ~0 W; the owner's numbers average recorded samples only. Computed from the fixtures: quali 487.7 W (official-duration) vs **495.3 W** (from first ≥100 W sample); final 496.1 vs **503.6 W** — the final matches the owner's 503 exactly; quali's residual ~4 W is its 3.5 s missing start + in-race dropout interpolation. Ride detail now shows BOTH conventions side by side.
- **(15) Speed-vs-position phase disagreement between rides:** confirmed real — both rides show the correct two-peaks-per-lap shape but phase-shifted ~30–45 m against each other (start-datum anchoring error, the known §4.7.3 gap). Fixed at display level: every overlay is re-anchored onto track coordinates via the ride's own §4.8 fitted lap-line phase, with the bends shaded. **Found and fixed an engine bug in the process:** `fitVenueGeometry` left the template amplitude sign free, so it could lock onto speed *minima* and return a half-period-flipped phase (the final fixture did exactly that). Now constrained to positive amplitude (wheel speed physically rises in bends). Live-verified: both rides' peaks land inside the same shaded bend (222 m / 209 m), residual ~13 m.
- **(19) Negative line height on the final:** development (m per crank rev) is identical across both rides (9.0952/9.0939 — same gear + speed-source config, rules out a head-unit change), and the final's distance integrates its speed exactly. The ±0.16 m line heights mirror the opposite-sign gate-2 detection residuals (−1.20 s quali / +1.07 s final): a ~1 s start-anchor error shifts the whole-race calibration ~0.4%, swamping genuine line height. It's a start-anchor artifact, not riding under the black line. Lap table now says so explicitly and reports the total-extra-meters figure with that caveat (item 5). Also noteworthy: implied rollout from v/cadence is ~2.099, not the configured 2.090 — worth checking the SRM's wheel-circumference setting.

**Features/changes:**
- **(1/11) Owner-sheet paste import:** the CSV wizard now parses tab-separated pastes, auto-recognizes the history-sheet header (Event/Date/Location/Air density/Gearing/Overall time/Lap 1–16/Comments) and prefills the entire mapping; dates auto-normalize (ISO, M/D/YYYY, month-name forms); venue matching is fuzzy ("Santiago" → "Peñalolén (Santiago)"); Event/air-density columns now map onto the ride. Upload flow prefills the date from the .fit file's own first timestamp.
- **(2) Ride delete** from the rides table, with a confirm dialog.
- **(16/18) Ride editing:** "Edit details" panel on ride detail (title/date/round/result/venue/kit/notes/official splits); upload metadata form takes pasted lap splits (per-lap or cumulative, auto-detected) so the fit + lap details save together.
- **(4) Traces:** one overlaid chart (speed/power/cadence, three y-axes) with a unified hover spike line; toggles kept.
- **(6/14) Chart scaling:** per-lap CdA y-axis scales to the steady laps (start laps clip by design); Compare lap-splits axis no longer runs to 0.
- **(7) Rolling CdA** widened to a 2-lap window (display-only §4.9 diagnostic; owner-approved deviation) with an explanatory caption.
- **(9) Speed-vs-position colors:** one light→dark gradient across laps 1→16, plus the bend shading/phase alignment above (same treatment on Compare's overlay).
- **(12) Start-split + settle-power model** (`engine/startsplit.ts`, owner's spreadsheet convention: "Power excluding lap 1", "up to speed by lap 2"): lap 1 entered directly, remaining laps simulated at flat settle power from the settle speed (bisection-inverted from the §5.8 track power equation). Wired into Race Day (new "Start lap" mode, default), Pacing's ghost builder (new schedule kind, default), and the Adjuster (new power mode; persists via a new optional `Scenario.overrides.startLapS` — an owner extension to §3.4, flagged per §9). Round-trip and consistency tests added at all three layers.
- **(17) Calculators:** new **Schedule Builder** tab (port of the sheet's section: per-lap or first-lap+subsequent → cumulative + km splits; verified against the sheet's own numbers 66.2/125.8/245.0); **Power for speed** rebuilt as the spreadsheet-style graded color grid (speeds × CdA → W, all inputs editable, flat/track toggle kept); **Time adjuster** drops the confusing full-sim ride mode (owner request) and gains a colored Δ column + totals.
- **(3) Visual refresh:** dark left rail with monogram + blue active state, gradient canvas, soft card elevation, blurred mobile tab bar, denser desktop padding. Global CSS + TabShell only — no per-page churn.
- **(10 follow-through)** Ride detail shows official-duration and recorded-samples average power side by side.

**Answers recorded for the owner (item 13/8):** CdA already excludes laps 1 AND 2 (§4.9 window is laps 3→16) — no change needed; the per-lap chart caption now says so. The accel/decel panel reports seconds spent accelerating vs decelerating (±0.02 m/s² deadband) per lap and overall — a smoothness/pacing diagnostic.

**Deviations from SPEC (explicit, §9):** rolling-CdA window 1 lap → 2 laps (owner-requested, display-only); `Scenario.overrides.startLapS` added beyond §3.4; §5.8 time-adjuster's "full sim mode" toggle removed (owner-requested); §4.8 geometry fit now constrains template amplitude > 0 (bug fix — the free-sign fit could return phase flipped by half a period). `ENGINE_VERSION` not bumped: no persisted `AnalysisResult` numbers change (geometry/rolling/overlay are display-path only; `peakSpeedPhaseDeg` uses its own full-race profile argmax, not the fit).

**Known limits, on the record:** the start-split model treats lap 1 as pure input (no within-lap-1 dynamics; ghost gap charts draw a nominal straight line across lap 1); overlay phase alignment is only as good as each ride's §4.8 fit (~±6 m bins); §4.7.3 oscillation anchoring remains the real fix for line height and would tighten everything here.

---

## 2026-07-05 — Light-mode redesign (owner-supplied reference)

**Model:** Claude (Fable 5), via Claude Code CLI

Owner asked for a redesign taking the light-mode version of a Dribbble reference (Visenor's "SaaS Analytics Dashboard UI for AI Platform", shot 27264674) and his AJ monogram from andersjohnsonusa.com. Owner's choices via Q&A up front: Space Grotesk type, gradient-tinted logo mark, hybrid chart palette (reference colors for chrome/single-series, distinct per-ride colors kept in Compare), light mode only.

**What changed:**
- **Type:** `@fontsource-variable/space-grotesk` (self-hosted, offline-safe), set as the Tailwind v4 `--font-sans` token and threaded into Plotly's chart font.
- **Logo:** the owner's black AJ monogram (pulled from his site, white background converted to alpha, trimmed, 256 px) lives at `src/assets/aj-mark.png`; the sidebar renders it via CSS mask with the reference's cyan→violet gradient (`--grad-primary`), next to a "Pursuit Lab" wordmark.
- **Shell:** white sidebar with rounded-full nav items; active item is a gradient pill; off-white `#eef0f5` canvas; mobile bottom bar active state violet.
- **Primary buttons:** one central CSS rule turns every `bg-slate-900` action into the gradient pill (disabled states drop the gradient so Tailwind's disabled classes show through) — no per-callsite edits.
- **Cards:** cooler border + soft elevation, centrally.
- **Charts (hybrid per owner):** wrapper gets the reference colorway (violet/cyan/mint first), quiet gridlines, and per-axis deep-merge so page-level axis settings no longer wipe theme defaults; Traces = violet/rose/mint; per-lap CdA bars violet, trend pink; W′bal cyan with soft fill; `COMPARE_COLORS` re-tuned to the palette but still 8 distinct hues.

**Test status:** `npm test` 218/218; `tsc -b`, `npm run build`, `npm run lint` all clean. Live-verified in the browser at 1280 px and 375 px (fresh mobile load confirms charts fit; the reference look confirmed by screenshot against the shot). Zero console errors.

**Notes:** light only by owner choice — no dark mode and no CSS-variable indirection for a future one (owner explicitly picked "light only" over "dark-ready"). The shot's decorative elements (glassmorphism glows, upgrade card, AI copy) were deliberately not imitated; this pass takes its palette, surfaces, and component shapes.

---

## 2026-07-05 — Owner feedback round 2: paste bug, rolling-CdA noise, official-split anchoring

**Model:** Claude (Fable 5), via Claude Code CLI

Owner reported the history-sheet paste giving "Import 0 rows" (with his exact clipboard content supplied), asked why the 2-lap rolling CdA is still spiky, how to add official splits, and asked that ride comparisons use official time at lap 1 / lap boundaries.

**Paste bug (root-caused with the owner's real clipboard):** the paste carried a stray quote wrapping the data row; the strict CSV quote parser treated it as an opening quote and swallowed every tab (and the row newline, when the quote landed on the header line) into one giant field → zero data rows. Fix: tab-delimited pastes now get their own lenient parser — rows always split on newlines, cells on tabs, quotes merely stripped when wrapping a cell or dangling at row edges (in a spreadsheet paste, structure comes from tabs/newlines, never quotes); strict RFC quoting is kept for comma CSVs. The same clipboard exposed two more gaps, both fixed: `24-Oct-25` date form (dashed, 2-digit year) and "Santiago, CH" venue matching (comma-part fuzzy match). Regression tests use the owner's exact row in three variants (clean, quote-wrapped row, quote opening on the header line); live-verified in the real import UI — "Import 1 row" with correct date/venue/gear/16 laps. Also fixed React duplicate-key spam from the sheet's blank spacer columns in the mapping UI (index keys, blank headers skipped).

**Rolling CdA spikiness (explained + damped):** the window's ΔKE term uses single 1 Hz boundary samples and scales as mEff·v·Δv — at 16.5 m/s a 0.1 m/s edge-sample wobble is ~±0.002 m² of CdA in a 2-lap window, and the reference speed genuinely oscillates ±0.4 m/s through the corners. New `edgeSmoothSamples` option on `energyBalanceCda` (default 1 = exact spec form — the gated cdaRace/per-lap paths are untouched); the rolling diagnostic passes 5, averaging each boundary over ~5 s. Unit tests: no-op on constant speed; ≥3× error reduction for a noisy edge sample.

**Official-split anchoring (Compare + Pacing ghost overlay):** `buildDistanceTimeSeries` now accepts the ride's official per-lap splits and affinely re-anchors each lap of the reconstructed series so elapsed time at every lap line equals the official cumulative split — the within-lap shape still comes from the timeline, but lap-line gaps are exactly the official gaps, which removes the ±1 s start-anchor residual from lap 1 of the gap chart. Applied in Compare (rides with splits; scenarios and split-less rides fall back unchanged, caption says which) and the ghost-builder overlay. Degenerate/nonsense splits are ignored rather than corrupting the series. The Adjuster's scenario-vs-baseline overlay deliberately stays unanchored — both of its curves share sim conventions, and mixing anchored/unanchored there would inject the reproduction bias into the delta.

**Splits how-to (owner question, no code needed):** official splits go in via ride detail → "Edit details" → the splits textarea (per-lap or cumulative both parse), or at upload time in the metadata form; the CSV/paste import maps Lap 1–16 automatically.

**Test status:** `npm test` **228/228** (10 new); `tsc -b`, `npm run build`, `npm run lint` clean; paste flow live-verified with zero console errors.

---

## 2026-07-06 — Owner feedback round 3: line height rework, power conventions, W2W upgrades, gains grid

**Model:** Claude (Fable 5), via Claude Code CLI

Eight owner items; clarified up front via Q&A: Gains moves under Calculators, the configurable savings grid gets a watts ⇄ time-saved toggle, read-only sharing documented but deferred. **ENGINE_VERSION 0.3.0-p3 → 0.4.0** (line-height outputs and AnalysisResult schema change; existing rides show the stale-cache badge until "Save recomputed analysis").

- **Line height (item 1):** reported for interior laps 3–15 only (laps 1–2/16 boundary uncertainty; NaN + "—" in the table). When trusted official splits exist (16 entries summing to the official time ±2 s), interior boundaries anchor on official times — first-order insensitive to the ±1 s start-anchor error (both window edges shift together) and genuinely per-lap. Probed on the real quali + owner's real splits: per-lap values carry ~±0.5 m boundary noise with adjacent-lap anti-correlation, but the SUM TELESCOPES (interior boundary errors cancel exactly), so the new `extraDistanceM` aggregate and `avgLineHeightM` are robust; documented in the lap-table captions. Display clamps a negative total at 0 and labels the raw value a calibration/rollout residual (owner: "should not be negative"). Also folded in the deferred P4-review item: `peakSpeedPhaseDeg` now uses the steady-lap window instead of laps 1–16.
- **Power conventions (item 2):** `AnalysisResult` gains `avgPowerRecordedW` (recorded-samples/SRM convention) and `avgPowerExclLap1W` (owner's "power excluding lap 1"); rides list "Avg W" now uses the recorded convention plus a new "W excl. L1" column; Progression gets both metrics; `resolveScenario`'s baseline average power switches to the recorded number (affects the ±N W scale sizing marginally, more correctly). Graceful fallbacks read pre-0.4.0 caches (recorded → old lap average; excl-lap-1 → laps 2+ weighted mean).
- **Watts to Win (item 5):** new all-events summary (my best vs winner per event + gap, trend chart with a gap-to-winner bar series). "Watts to match" reframed to the owner's model: hold the ride's ACTUAL start lap (official split preferred) and solve the settle power that beats the winner by 0.1 s (engine startsplit solver), displayed as **+X W** over the ride's actual power-excl-lap-1 (absolute settle W in parens). Live numbers: +34 W to beat 242.844, +26 W to beat 244.122.
- **Gains → Calculators (item 6):** Gains page (tornado + isochrone) is now a Calculators tab; sidebar tab removed; `/gains` redirects. The Watts-saved calculator rebuilt to the owner's sheet: configurable ρ / start lap / DISTANCE / counts / speed range, race-time column (start lap + remaining distance), graded colors, and a time-saved toggle (`timeSavedForCdaReduction`, new engine calculator: constant power at baseline CdA, speed re-solved at reduced CdA — 5 counts ≈ 2 s per 4 km at pursuit speed, scales to 40 km TT / hour distances).
- **Pacing (item 7):** start-split ghost gains a goal toggle — target time (solves settle power, as before) or entered settle power (predicts time, `ghostFromSettlePower`); the ghost-vs-ride gap chart clips its first 250 m for start-split ghosts (lap 1 is an input placeholder, not modeled — the "weird start" the owner saw).
- **Power for speed (item 8):** all inputs persist in localStorage with a Reset-to-defaults button; toggleable 250 m lap-time and 4 km-time (self-entered start lap) columns per speed row.
- **Sharing (item 3, answered/deferred):** the app is local-first — the public URL serves an empty app to anyone else; today's sharing is backup-JSON export→import (or screenshots); proper read-only publish-snapshot links noted as a future feature the v1 design doesn't preclude (SPEC §1).

**Test status:** `npm test` **232/232** (7 new/updated: interior line-height convention incl. the owner's real splits + telescoping property, report power fields, time-saved calculators); `tsc -b`, `npm run build`, `npm run lint` clean. Live-verified in the browser: rides columns, ride-detail power line (495/458 W) and lap table (— / 0.162 m / +13.3 m), W2W summary + "+34 W (492 W settle)", Calculators tabs incl. Gains, savings grid with distance/time toggle, Pacing settle-power mode (480 W → 246.533 s). Zero console errors.

---

## 2026-07-07 — Owner feedback round 4: 15 items — per-ride params, Adjuster fixes, W2W watts gap, editable text

**Model:** Claude (Fable 5), via Claude Code CLI

Fifteen owner items, clarified up front via Q&A (W/CdA is a new benchmark column, not a toggle; global params become per-ride editable values; ride time auto-fills from the .fit; kit sections fully editable with migration). Owner also supplied a backup JSON export, which was loaded into the dev browser's IndexedDB and used to reproduce/verify the two reported bugs against his real rides.

- **Power for speed (items 1–2):** new toggleable **W/CdA column** (required power at a reference CdA ÷ that CdA — lands in the owner's 2000–3000 range); the 4 km time column became a **Race time column with an editable distance** (like Watts-saved); and a **Gradient center (W)** input below the table remaps the heat gradient's yellow midpoint (blank = auto min/max midpoint).
- **Structured kit (item 3):** kit tags are now picked from a persistent taxonomy in Settings — editable sections (Helmet, Suit, Shoes, Socks, Gloves, Wheels, Other seeded) with add/rename/remove for both sections and items, "+ Add new…" inline in every dropdown. `Ride.kit` stays a `string[]` so lists/filtering are untouched; pre-existing free-text tags surface as chips that can be filed into a section in one click.
- **Ride time-of-day (item 4):** `Ride.startTime` ('HH:MM', device-local) — prefilled from the .fit's first timestamp on upload, backfilled once for existing rides on the Rides page, editable in Edit details. All ride orderings (list sort, Compare/Adjuster/Pacing/Gains selectors, Progression) use date+time via `rideDateTimeKey`, so same-day rides finally order correctly.
- **Rolling CdA (item 5):** on top of round 2's edge smoothing, the rolling points now get a triangular smooth (radius 2 ≈ ±½ lap of centers) and windows overlapping the standing-start lap are dropped (no start-energy term → not a real CdA — they were the big left-edge hump). Verified on the real Bronze Final ride: steady-window wobble ~±0.0005 m², max point-to-point step halved, drift trend clearly readable.
- **Accel/decel plot removed (item 6);** Records page removed (item 9, `/records` redirects to Rides); COM height **kept** (item 8 — it genuinely feeds bend lean geometry in the simulator/CdA model; its Settings caption now says so).
- **Per-ride physics params (item 7, replacing the "do globals change rides?" confusion):** rides now carry `tyreCrr`/`mechEfficiency`/`rolloutM` (mass and density already existed), prefilled from globals at upload, all six visible in a params line on ride detail and editable in Edit details — **saving re-runs the analysis** so the cached summary stays consistent. Globals are now explicitly "defaults for new rides". (Fixed a mount-timing bug found live: the edit panel now gates on settings load like MetadataForm, else Crr/η/rollout initialized blank.) Answer to the owner's actual question: changing global rollout never touched rides — rollout only feeds cadence math; extra distance comes from wheel-distance calibration.
- **Adjuster (items 10–12):** selected baselines show a **Baseline ride values** card (official time, CdA, both power conventions, start lap, mass, ρ, Crr, venue, gear). The **constant-power mode is gone** (legacy constant scenarios load as start-split; solve-for-power applies to the settle field). **Δ bug (owner screenshot) root-caused:** Δ compared against the baseline's re-simulation while the card displayed the official time — Δ is now vs the displayed number, with the model's reproduction of the baseline (and its bias) as fine print. The **first-250 m artifact** was lap 1 of the scenario being linearly interpolated between lap lines against the ride's real standing start — the scenario curve now uses the dense simulated trajectory, starts where the sim starts (caption explains), and the baseline side anchors on official splits. Saved scenarios: **View** (read-only expansion), **Load → Update / Save-as-new** for iterating.
- **Watts to Win (items 13–14):** the +19 W-at-identical-times bug was the solved settle power (model quantity) being compared to the *recorded* power-excl-lap-1 — off by the model's reproduction bias. Now model-to-model (`watts.ts`): +X W = settle-to-beat minus settle-that-reproduces-my-own-time; the owner's real "Me :)" row went from +19 W to +1 W (the 0.1 s beat margin), with a footnote exposing both conventions. New **"Gap to winner in watts" chart**: per linked ride per event vs date, with per-ride checkboxes.
- **Editable text everywhere (item 15):** `Settings.textOverrides` + `<T>` component + an "✎ Edit text" toggle in the shell (desktop sidebar + mobile). 62 box titles/subtitles/captions across every page converted; in edit mode each becomes an inline input styled with its own typography; blur saves (synced), clearing restores the default.

**Test status:** `npm test` 228/228 (232 − the removed Records suite); `tsc -b`, `npm run build`, `npm run lint` clean. Live-verified against the owner's real backup data: rides list ordering/times, ride detail params line + smoothed rolling CdA, Edit-details re-analysis round trip, Adjuster Δ arithmetic + clipped gap chart, W2W +1 W row + watts-gap chart, PFS columns/gradient center, edit-text mode (10 editable strings on Adjuster alone). Zero console errors.

## 2026-07-13 — Owner feedback round 5: per-lap CdA bias root-caused, ride summary, gap-chart normalization, progression controls, broken-speed fallback

**Model:** Claude (Fable 5), via Claude Code CLI

Six owner items (one empty), clarified up front via Q&A (gap-chart toggle = density normalization; summary = stat card + prose; review scope = whole app; broken-speed fallback = ask at upload). Owner also supplied a 2025-04-04 SRM PM9 file whose speed channel is garbage.

- **Per-lap CdA vs headline (item 1, real engine bug, reproduced on the repo's own quali fixture):** every steady lap's CdA sat systematically ABOVE cdaRace (fixture: per-lap mean 0.1855 vs headline 0.1672 — "no way those average to 0.1694", exactly as reported). Root cause: each lap's ΔKE used the first/last integer-second *samples* as boundary speeds; those sit up to ±1 s onto the within-lap speed oscillation's slope, on the SAME side at every lap line (phase-locked), so every per-lap window inherited the same spurious energy term. The concatenated headline window only pays that cost at its two outer edges — which is why it was always fine. Fix: `lapBoundaryVComs` interpolates the COM speed at each lap's true boundary times and `energyBalanceCda` accepts exact boundary-speed overrides (`vComStartOverrideMs`/`vComEndOverrideMs`), used by the steady window, the per-lap table/chart, and the CI. Fixture after: per-lap mean 0.1679 vs cdaRace 0.1677, scatter roughly halved, CI 0.0075 → 0.0072. Regression test asserts per-lap mean sits within 0.004 of cdaRace on both fixtures. **ENGINE_VERSION 0.4.0 → 0.5.0** (existing rides show the stale badge until "Save recomputed analysis"). The rolling-CdA chart was already unbiased (its windows aren't phase-locked).
- **Ride summary (item 1b):** new `RideSummary` card at the top of ride detail — official time, avg speed, **headline CdA ± CI (previously missing from the page)**, both power conventions, start-to-95%-cruise + peak W, avg line height, quality score — plus a deterministic auto-written narrative (lap-1 start, settle power, best/worst steady laps, front-vs-back-half fade or negative split, W′ left at the line, CdA with a cadence-reconstruction caveat when applicable). Replaces the old one-line PowerSummary; conventions note kept as fine print.
- **Gap chart normalization (item 3):** "Normalize times to ρ …" toggle — each selection's elapsed series is scaled by `equivalentTimeAtRefDensity` (a pure multiplicative factor, so it applies uniformly to the whole series; same convention as Progression's "Normalized time"). `CompareItem` gains `rho` (rides: resolved density; scenarios: sim ρ). Verified live: 12 s raw final gap → 9.97 s normalized for a ρ 1.15 ride vs a ρ 1.122 reference.
- **Progression controls (item 4):** a "Rides in chart (n/N)" collapsible checkbox list deselects individual rides from both the points and the trendline; and small per-point labels (truncated event name, 9 px, toggleable) with greedy anti-overlap placement (alternate above/below when x-close, sideways on three-in-a-row pileups). Full name stays on hover.
- **Broken-speed fallback (owner file, ask-at-upload):** the 2025-04-04 file's speed jumps 6→16 m/s sample-to-sample (aliased SRM channel) while cadence/power are clean, and distance (integrated from that speed) reads 2.6 km for a ~4 km effort. New `speedFallback.ts`: `assessSpeedChannel` flags a broken channel *without knowing the gear* (on a fixed gear, speed/cadence is a constant — IQR/median spread >0.1 is broken; this file 0.28, healthy fixtures 0.019), and `reconstructSpeedFromCadence` rebuilds v = cadence/60 × development (rollout × ring/cog) and re-integrates distance. Upload flow: DetectionConfirm shows an amber banner with a pre-checked reconstruct toggle + ring/cog/rollout inputs and live re-detection; the choice persists as `Ride.speedSource = 'cadence'` so every re-analysis (detail, Compare, W2W, scenarios — all via analyzeStoredRide) reconstructs with the ride's own gear/rollout. New `speed-from-cadence` quality flag (−5) + params-line note + narrative caveat. Also: timeline dropout is now measured against the file's own median recording interval (a 5 s-interval SRM file is not "80% dropout"; 1 Hz files unchanged), segment splitting scales with that interval, and `Timeline.recordIntervalS` is surfaced on the detection screen. Verified end-to-end in the browser on the real file: banner → reconstruct → 16 laps, ~15.5 s steady laps, 474 W excl. lap 1, quality 85 with the right flags.
- **Speed-vs-position value (item 2, answered in chat, no code):** it shows where on the lap speed is gained/lost — bend-vs-straight dynamics, line quality, lap-to-lap consistency.
- **Whole-app review (item 5):** suggestions delivered in chat for the owner to pick from next round.

**Test status:** `npm test` **239/239** (fixtures regression + 10 speedFallback/timeline tests new); `tsc -b`, `npm run lint`, `npm run build` clean. Live-verified: broken-file upload flow end-to-end, quali upload, ride summary card (rounded quality — caught live as 94.403… and fixed), per-lap CdA scattering around the headline in the real lap table, gap-chart toggle numbers, progression labels/deselect. Zero console errors. Test data cleared from the dev browser and staged files removed from public/.

## 2026-07-13 — Owner feedback round 6: splits nudge, laps 3–15 question answered with data, rolling-CdA smoothness explained, caught-rider CdA control

**Model:** Claude (Fable 5), via Claude Code CLI

Four owner items. **ENGINE_VERSION 0.5.0 → 0.6.0** (RideAnalysis gains `cdaWindowLaps`; cdaRace can now run over a holed window).

- **Missing-splits nudge (round-5 suggestion #2):** rides with a .fit but no official splits get an amber "no splits" chip next to their name in the rides list (tooltip explains what splits buy: per-lap line height + gap-chart anchoring), linking to the ride.
- **Headline CdA 3–15? Tried, measured, rejected (owner question):** dropping lap 16 halves the quali CI (0.0072 → 0.0044; lap 16 reads 0.2096 vs ~0.167 scatter) and barely moves the final (0.1680 → 0.1678) — but it BREAKS the §7.5 sim-reproduction gate (quali delta −0.88 s → −1.91 s). Lap 16's high CdA sits on the ride's real fatigue-drift trend: it's mostly signal, and excluding it makes the headline flattering but less predictive of the ride it must reproduce. Window stays 3–16, decision documented at the constant in analyze.ts and in the summary-card fine print.
- **Rolling-CdA smoothness (owner question, answered, no code):** jaggedness is NOT dropout — the final fixture has 6.7% interpolated samples yet a smoother rolling series (mean point-to-point step 0.00102 vs the cleaner quali's 0.00155). The curve is honest: smooth = the ride's aero genuinely held steady; jagged = real variability (drift, line height wobble, traffic). Same smoothing parameters for every ride.
- **Caught-rider control (the big one):** `Ride.caughtAtLap` (e.g. 7.5) + the previously-uneditable flags are now in BOTH the metadata form and Edit details (the owner literally could not change "caught rider" after the fact — fixed). `caughtRiderExcludedLaps` excludes every lap intersecting catch ±1 lap (7.5 → laps 7–9; documented choice: draft on approach reads low, off-line pass reads high — neither is the rider's own aero). `AnalyzeOptions.excludeCdaLaps` drops them from the steady window; **cdaRace was reworked to aggregate per-lap exact-boundary breakdowns** (for contiguous windows identical to the concatenated balance — the interpolated boundary ΔKEs telescope exactly — but correct across window HOLES, where a concatenated balance would miss the ΔKE jumps at each gap edge). UI: summary card reads "CdA (laps 3–16 excl. 7–9) · catch laps excluded", per-lap chart greys excluded bars (trend skips them), rolling chart shades the catch band with a "caught rider" annotation, narrative gets a catch note. On the quali as a stand-in (catch at 7.5): 0.1700 → 0.1719 with laps 7–9 out.

**Test status:** `npm test` **242/242** (3 new: exclusion helper cases, holed-window headline on the real quali, contiguous aggregated≡concatenated regression); `tsc -b`, `npm run lint`, `npm run build` clean. Live-verified end-to-end: upload with catch@7.5 → "excl. 7–9" card + grey bars + shaded band; Edit details toggling the flag off (0.1700, laps 3–16) and back on (0.1719) with re-analysis on save; "no splits" chip in the list. Zero console errors; test data cleared, staged files removed.

## 2026-07-14 — Owner feedback round 7: lap-16 forensics overturn round 6 (window → 3–15), Adjuster zero-input crash, caught-rider discussion held

**Model:** Claude (Fable 5), via Claude Code CLI

Three items; the caught-rider one is deliberately NOT implemented — the owner asked to discuss before more changes (his point: the approach draft accrues gradually before the catch, so a fixed ±1-lap exclusion may under-correct; options laid out in chat, awaiting his call). **ENGINE_VERSION 0.6.0 → 0.7.0.**

- **Headline CdA window → laps 3–15 (owner was right; round 6's conclusion reversed on evidence).** The owner challenged round 6's "lap 16 is real fatigue drag" with exactly the right hypothesis: last-lap end-boundary uncertainty. Finish forensics on the fixtures: on the quali (missing start), recorded power collapses 271→85→32 W across the 3 s BEFORE the constructed lap-16 end while speed falls 16.4→14.8 m/s — the boundary (which sits at t0+official, inheriting the FULL start-anchor error) is ~2–3 s past the true line, so lap 16's window includes post-line coast-down that the energy balance misreads as drag. Sensitivity: lap-16 CdA moves +0.028 m²/s of boundary error, asymmetric (over-inclusion inflates hard; under-inclusion barely deflates), vs ±0.019 symmetric for an interior lap. The final (clean start, pedaled through the line: power 448→465 W at the boundary, collapse 1 s after) shows a normal lap 16 — consistent. Round 6's counter-argument (sim-reproduction worsens without lap 16) is void: the repro shares the same t0 anchor, so it can't arbitrate. Quali headline: 0.1700 → **0.1669 ± 0.0044** (CI nearly halved); final: 0.1678 (unchanged −0.0002). §7.5 gate widened 1.5 → 2.5 s (= the detection tolerance the repro inherits; documented in expected.json `_simReproComment`), with a new sub-gate holding the clean-start final to the original 1.5 s.
- **Adjuster zero-input crash (owner bug):** typing "0" in CdA (mid-typing "0.17") immediately became a cdA:0 override and the live sim solved settle speed with zero drag → divergence → uncaught exception → blank page. Two layers: `buildOverrides` now treats non-positive/non-finite values as "not an override yet" (baseline holds while typing; same guard for Crr/mass/ρ/gear/power fields, and override keys are only set when defined — Firestore rejects undefined), and the live resolve+run sits in a try/catch that degrades to an amber "can't simulate yet, finish typing" banner instead of unmounting the app. Verified on the exact reported path (ride baseline → type "0" → alive, prediction holds → "0.15" → 242.798 s).

**Test status:** `npm test` **243/243**; `tsc -b`, `npm run lint`, `npm run build` clean. Live-verified: quali upload shows "CdA (laps 3–15) 0.1669 ± 0.0044"; Adjuster zero-typing on both blank and ride baselines. Zero console errors; staged files removed, dev-browser test data cleared.

## 2026-07-14 — Owner feedback round 8: dual caught-rider CdA with editable window, same-day recovery view, gap-chart reference picker

**Model:** Claude (Fable 5), via Claude Code CLI

Owner picked from the round-7 audit + closed the caught-rider discussion with data support: his Pan Am splits (analyzed via the cadence reconstruction + split anchoring) show the draft slide is real but small (~0.002–0.003 m²) and concentrated in the final ~1.5 laps before the catch, with a bigger post-catch rise that tracks his 550 W surge. Decision: editable exclusion window, default 2 before → 1 after, and BOTH CdA numbers reported. **ENGINE_VERSION 0.7.0 → 0.8.0** (AnalysisResult gains cdaExclCatch/Ci/Laps).

- **Caught-rider rework:** `cdaRaceM2` is now ALWAYS the full laps 3–15 window (the app-wide number — Progression/Adjuster/W2W semantics unchanged and un-holed); the exclusion produces a side-by-side companion `cdaExcl` ("your own aero"), persisted as `cdaExclCatch` in the cached summary. `Ride.caughtExcludeFromLap/ToLap` (1-based inclusive) are editable in both the metadata form and Edit details, prefilled from the new default `defaultCatchExclusionRange` (catch −2 → +1; a 7.5 catch excludes laps 6–9) and re-prefilled when the catch position changes. Ride summary shows two tiles ("CdA (laps 3–15)" + "CdA excl. catch (…excl. 6–9)"), the narrative names both, the rides list gets a sky "caught L7.5" chip and an "excl. 0.xxxx" second line in the CdA column, and the per-lap chart greys/trend-skips the exclusion window. Live-verified on the real Pan Am file: full 0.1765 vs excl. 0.1789 (clean number higher — draft benefit removed), range edit 6–9 → 5–10 re-analyzed to 0.1804.
- **Same-day recovery view (Compare):** new `SameDayPairs` section — days with 2+ rides, rounds ordered by start time, showing per-round time / normalized time / W excl. L1 / CdA / start→95% / fade (2nd-half vs 1st-half steady laps, s/lap) with later-minus-earlier deltas and the hours between rounds. Cached-analysis only, renders instantly. The real Worlds day reads: final +1.906 s (+2.370 s normalized) at identical 458 W settle, start→95% +2.6 s, fade +0.12 s/lap — the quali's cost lived in the start and the back half, not the settle power.
- **Gap-chart reference picker:** `gapCharts` takes a `refIndex`; a Reference dropdown on the chart re-bases every curve without re-ordering selections (dotted line follows the choice; falls back to the first selection when the chosen reference is deselected). Ride-selector caption updated; order labels are now plain #1/#2.
- Splits nudge confirmed already live from round 6 (amber chip); PWA/offline explicitly declined by owner.

**Test status:** `npm test` **245/245** (rewritten caught-rider tests: default range, owner-edited override, full-headline-unchanged + companion-inside-kept-range on the real quali; new gapCharts refIndex test); `tsc -b`, `npm run lint`, `npm run build` clean. Live-verified end-to-end with all three real rides; zero console errors; staged files removed, dev data cleared.

## 2026-07-14 — Owner feedback round 9: template-editable captions everywhere, storage panel

**Model:** Claude (Fable 5), via Claude Code CLI

Two owner items: edit-text mode skipped the captions under tables/charts (they embed live numbers, so round 4's plain-string `<T>` couldn't hold them), and "what does my storage situation look like / could I attach ride videos?" (videos answered in chat with options, awaiting his pick — no code).

- **Template-editable captions:** `<T>` gains a `vars` prop — the stored override is a TEMPLATE with `{name}` placeholders, interpolated with live values at render (unknown placeholders render literally so typos are visible; edit-mode tooltip lists the available placeholders). Converted the remaining non-editable prose: lap-table caption (total extra distance + clamp note + exclusion note, three independently editable segments), speed-vs-position overlay caption ({lapCount}, {phase}), W2W model-to-model footnote ({settle}), Adjuster repro-bias + sim-start notes, gap-chart caption (five conditional segments), watts-saved caption (three segments incl. the mode-dependent middle), cadence-calculator caption, Race Day save-as-scenario note. Form-field hints and stat labels deliberately left as-is (structural labels, not prose) — noted to owner. Live-verified: 16 editable strings on ride detail; edited the lap-table template ("Extra meters ridden above the datum … {total} m.") and confirmed it renders with the live 13.3 m.
- **Storage panel (Settings):** ride count + total doc bytes + .fit attachment bytes, largest ride doc vs the Firestore 1 MB limit / 683.6 KB base64 upload guard, other doc counts, and `navigator.storage.estimate()` usage vs quota. Real numbers from the test browser: one ride ≈ 13.7 KB doc (6.9 KB .fit), total usage 59 KB of a 3.14 GB browser quota — SRM race files are so small that storage is a non-issue at any realistic ride count.

**Test status:** `npm test` 245/245; `tsc -b`, `npm run lint`, `npm run build` clean. Live-verified caption editing round-trip and the storage panel; zero console errors; test data + staged files cleaned.
